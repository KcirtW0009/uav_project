第2章 系统建模与业务感知的增强切换算法（8.4k)
2.1无人机辅助通信网络场景建模
2.1.1信道模型与SINR计算
信道模型综合考虑大尺度路径损耗与小尺度瑞利衰落。于无人机与基站之间的路径损耗（单位：dB）基于对数距离模型：
		（2-1）
		（2-2）
	 
其中为无人机到基站的欧氏距离（km）。该模型参照3GPP TR 36.777中关于空中终端信道特性的规范建议[30]。
在此基础上，引入瑞利衰落以模拟多径效应造成的信号幅度波动。综合路径损耗与衰落后的信干噪比（SINR）计算公式：
		（2-3）
其中dBm为基站发射功率， dBm为热噪声功率谱密度对应的噪声基底。SINR矩阵 每步更新，作为切换决策的核心输入。
为进一步建立链路质量与服务体验之间的定量映射关系，本文设计了基于SINR的时延估算模型与丢包率估算模型。
基于SINR的时延估算模型为：
		（2-4）
其中为基准时延（为业务允许的最大时延）。当SINR从10dB降至-5dB时，时延从约攀升至超过业务容忍上限。
丢包率的估计采用指数衰减模型：
		（2-5）
高SINR时丢包率趋近于，低SINR（-5dB）时约15%，为满意度评估提供合理输入。
2.1.2 基站容量与负载模型
基站的标称容量的参考5G基站的实际能力设定（宏站1~2Gbps，微站约500Mbps）[31]。仿真中容量从场景特定区间均匀采样，使平均负载率稳定在约77%，以平衡资源竞争与断连风险。
各场景容量范围分别为：默认[500,1000]、智慧城市[1500,2400]、工业巡检[1400,2300]、应急救援[900,1200]、农业植保[600,900]、物流配送[1200,2100]（单位：Mbps）。
基站负载率和可用容量定义为：
		（2-6）
	其中为时刻连接至基站的无人机集合，为无人机当前获得的分配速率。此外，基站故障状态下可用容量归零，已连接无人机被强制释放，用于模拟随机事件。
资源分配默认“先到先得”，增强算法引入抢占机制：高优先级用户需求无法满足时，逐出低优先级用户并触发其软迁移。这构成了增强算法的物理基础。
2.2业务类型差异化QoS需求建模
2.2.1 三类典型UAV业务的QoS特征定义
面向无人机差异化服务保障的研究目标，本文选取控制信令、视频回传、环境监测三类业务，分别对应3GPP TS 22.125中的URLLC、eMBB、mMTC[32]。其QoS需求的显著差异是业务感知切换算法的设计动机。
控制信令业务（Control Signal）属于URLLC类别，承载遥控指令与遥测数据，具有极高可靠性和严苛时延约束，但数据量极小。其QoS参数：最低保障速率Mbps，理想速率Mbps，最大允许时延ms，最大允许丢包率，优先级，关键程度，降级容忍度，时延敏感度。上述参数意味着控制信令业务对时延极其敏感（权重最高），仅能接受最多5%的服务降级，一旦关键性能指标不满足即判定为完全失效。在网络流量特征层面，控制信令呈现低带宽（均值约500kbps）、低时延（10±3ms）、极低抖动（1±0.5ms）以及近乎为零的丢包率（Beta(1,1000)分布乘以量级缩放因子）的特点。
视频回传业务（Video Streaming）属于eMBB类别，是当前无人机应用中最为常见的数据密集型业务形态，广泛应用于城市监控、影视航拍、电力线路巡检等需要高清实时画面传输的场景。其QoS参数配置为：最低保障速率Mbps（支持720P以上分辨率编码），理想速率Mbps（支持4K超清画质），最大允许时延ms，最大允许丢包率，优先级，关键程度，降级容忍度，时延敏感度。相比控制信令，视频回传业务对带宽的需求高出两个数量级，但仍要求相对较低的传输时延以保证画面的流畅性和实时交互性。其流量特征表现为高带宽（50±15Mbps）、中等时延（15±5ms）、低丢包率和低抖动（3±1ms）。值得注意的是，视频业务的降级容忍度为0.35，意味着可以接受一定程度的码率自适应下调（如从4K降至1080P），这为后续增强算法中的降级搜索机制提供了操作空间。
环境监测业务（Environment Monitoring）属于mMTC类别，适用于农业传感器、大气监测等大规模物联网。此类业务的数据包通常体积极小、发送频率低但对连接数规模有一定要求。其QoS配置为：最低保障速率Mbps，理想速率Mbps，最大允许时延ms（宽松至1秒级别），最大允许丢包率，优先级，关键程度，降级容忍度，时延敏感度。环境监测业务最显著的特征在于其对时延的极度宽容（时延敏感度仅为0.2）和对服务降级的高度容忍（最高可接受75%的速率缩减），这使得它成为负载均衡和资源调度中的"弹性缓冲池"。其流量特征对应为低带宽（1±0.3Mbps）、高时延（500±200ms，反映传感器数据的汇聚上传模式）和高抖动（50±20ms）。
表2-1汇总了三类业务的全部QoS参数及其对应的3GPP服务类别映射关系。
表2-1三类典型UAV业务的QoS参数配置
参数	控制信令 (URLLC)	视频回传 (eMBB)	环境监测 (mMTC)
最低保障速率 (Mbps)	0.15	25	0.5
理想速率 (Mbps)	0.5	50	1.0
最大允许时延 (ms)	20	20	1000
最大允许丢包率 (%)	1	5	5
优先级	0.99	0.75	0.30
关键程度	1.0	0.7	0.3
降级容忍度	0.05	0.35	0.75
时延敏感度	1.0	0.8	0.2
流量特征：时延 (ms)	10±3	15±5	500±200
流量特征：带宽 (Mbps)	0.5±0.1	50±15	1.0±0.3

不同场景业务比例差异显著：应急救援85%控制信令（URLLC主导），智慧城市60%视频回传（eMBB主导），农业植保60%环境监测（mMTC主导），物流配送50%控制+40%视频+10%监测（均衡混合）。这些比例已在仿真环境中配置。
2.2.2 层次化满意度评估指标体系
层次化的多维度满意度评估体系的输入来自三个维度——速率满意度、时延满意度和丢包满意度，输出为一个归一化的满意度得分。
速率满意度的计算采用分业务类型的非线性阶梯函数，以精确刻画不同业务对速率变化的差异化响应行为。对于控制信令业务，速率满意度呈现高度非线性的"陡峭门槛"特征：当分配速率达到理想速率的95%以上时满意度为1.0（完全满足），降至95%~85%区间时线性下降至0.7，继续降至85%~70%区间时进一步下降至0.3，低于70%则骤降至0。这种设计反映了控制信令业务对资源不足的极端敏感性——任何显著偏离理想速率的情况都将严重损害服务质量。对于视频回传业务，速率满意度使用平滑过渡的三次Hermite插值函数（Smoothstep函数）来模拟码率自适应下的渐进式体验退化：90%以上为满分，70%~90%区间平滑过渡，50%~70%区间线性递减，50%以下按比例折算。对于环境监测业务，速率满意度最为宽松：80%以上即为满分，30%~80%区间线性过渡，体现了其对速率波动的高度容忍性。
时延满意度和丢包满意度的计算则分别采用统一比值模型：
		（2-7）
		（2-8）

其中为当前估计时延，为当前估计丢包率。
多维加权综合满意度通过时延敏感度驱动的动态权重合成：
		（2-9）
		（2-10）
		（2-11）
该权重方案确保了高时延敏感度的业务（如控制信令，）赋予时延维度更高权重，而低时延敏感度的业务（如环境监测，）则让速率维度占据主导地位。此外，当任一维度的满意度严重偏低（<0.2）时，整体满意度还将受到0.5倍的惩罚因子，以体现短板效应对用户体验的决定性影响。
在此基础之上，层次化评估体系进一步定义了多个聚合级别的统计指标，包括关键满意度、加权满意度、整体满意度及分业务满意度。
2.3 传统切换算法原理及其局限性分析
2.3.1 3GPP A3事件机制原理
传统蜂窝网络的切换决策主要依赖3GPP标准中定义的事件触发机制，其中A3事件（邻区优于服务小区）是最广泛采用的基础切换准则[32]。A3事件的触发条件可形式化表达为：
		（2-12）
	其中为邻区基站的参考信号接收功率或SINR，为当前服务小区的对应测量值， 为迟滞参数（Hysteresis）， 为频率偏移量（同频场景下为0）。本文设置迟滞参数dB，频率偏移dB。
算法的执行流程概括如下：在每个仿真步，遍历所有已连接的无人机，对于每架无人机扫描除当前服务小区外的所有邻区，记录SINR最高的候选目标小区。若候选小区的SINR超过A3事件阈值（即当前服务小区SINR加上迟滞量），则发起切换请求。切换执行采用"先断后连"（Break-Before-Make）方式：首先释放旧基站上的资源占用，随即向新基站申请资源分配。若分配成功则切换完成；若分配失败（目标基站无足够可用容量），由于旧连接已被释放且不存在回滚机制，该无人机进入断连状态。此外，算法还包括紧急切换逻辑：当服务小区SINR低于-5dB或当前满意度低于0.7时，跳过迟滞检查直接寻找更优基站。
2.3.2 在UAV差异化服务场景下的不足
将传统A3事件算法置于无人机差异化服务的仿真环境中进行测试，实验结果揭示了若干系统性缺陷，这些缺陷从根本上制约了该算法在复杂业务场景中的适用性。
其一，业务感知缺失导致资源错配。 传统算法无法识别无人机的业务类型，对所有UAV统一采用SINR最大化，无法区分控制信令的时延敏感与视频回传的带宽需求，导致高优先级业务可能被引导至重载基站。
其二，“先断后连”机制固有的断连风险。 A3事件所采用的标准切换流程为“先释放当前连接，再建立目标连接”，在资源竞争环境下存在内在的断连隐患。实验数据（参见第四章实验三结果）显示，传统基线算法的平均约每七次切换尝试中就有一次以断连告终。对于控制信令这类关键业务而言，每一次断连都可能造成指令丢失甚至飞行安全事故。
其三，缺乏弹性保障与负载均衡机制。 传统算法在切换失败后不具备任何恢复手段。一旦“先断后连”流程中的分配步骤失败，无人机即进入断连状态，只能等待下一决策周期重新尝试接入，在此期间服务完全中断，满意度骤降至零。此外，该算法也不具备主动的负载调节能力——当部分基站过载而另一些基站空闲时，不会触发任何均衡迁移操作，导致系统整体资源利用率不均衡。
其四，固定阈值设置的适应性不足。 传统算法中2 dB的固定迟滞参数在不同场景和不同业务条件下并非最优选择。类似地，紧急切换的固定阈值（SINR低于-5 dB或满意度低于0.7）也缺乏对业务优先级差异的精细适配。
综上所述，传统3GPP A3事件算法在设计之初主要面向地面低速移动用户的语音与数据业务场景，其隐含假设——单一业务类型、充足的网络资源、较低的移动速度——在无人机差异化服务场景中已不再成立。上述局限性构成了本文提出增强切换算法的直接动因。
2.4业务识别模块设计
2.4.1 特征工程与多维特征向量构建
业务识别模块在线判别每架无人机的业务类型，传递给切换决策算法。识别准确率直接影响系统性能：当准确率从100%降至33%时，整体满意度降幅达20.3%（见第四章实验一）。
识别模块的输入为每架无人机实时生成的四维特征向量，各维度定义如下：
		 	（2-13）

这四个特征维度分别对应业务流量的核心QoS属性，其数值分布由各业务类型的特征生成参数决定。具体而言，控制信令的特征分布为、、、；视频回传为、、、；环境监测为、、、。

图2-1业务识别特征散点图
如图2-1所示，三类业务在时延-带宽平面形成明显聚类，可分离性良好。这种天然的可分离性保证了分类器能够以较高的准确率完成识别任务。
在特征预处理阶段，所有输入特征经由StandardScaler进行标准化变换（零均值、单位方差），以消除不同量纲对距离型分类器的影响并加速模型收敛。
2.4.2 多分类器对比与最优模型选取
为筛选最适合在线推理场景的分类器，本文对五种主流机器学习算法进行了系统性的对比评估：决策树（Decision Tree, DT）、支持向量机（Support Vector Machine, SVM）、多层感知机（Multi-Layer Perceptron, MLP）、随机森林（Random Forest, RF）和梯度提升树（Gradient Boosting Decision Tree, GBDT）。各模型的超参数配置如下：DT的最大深度限制为12、最小分裂样本数为10；SVM采用RBF核函数、正则化参数、核系数使用'scale'自适应策略；MLP的隐藏层结构为(128, 64, 32)、启用早停机制防止过拟合、最大迭代次数1000；RF包含100棵 estimators、最大深度15；GBDT同样包含100棵 estimators、学习率0.1、最大深度5。
训练数据采用每类3000样本的平衡数据集（共9000条），按8:2比例划分为训练集和测试集，并保持分层抽样以维护各类别的原始比例。所有实验在全局随机种子（seed=30042）控制下进行以确保可复现性。
评估采用多目标综合评分方法，兼顾三个维度的性能：
		（2-14）
其中为测试集F1分数（准确性维度，权重40%），为五折交叉验证均值（稳定性维度，权重30%），为延迟评分（实时性维度，权重30%），为延迟惩罚因子——当单样本推理延迟超过10ms阈值时施加0.5倍惩罚以排除不适合在线部署的模型。
表2-2展示了五种分类器的详细对比结果。决策树综合评分0.969，F1=0.9993，交叉验证均值99.97%，延迟趋近0ms，1440个测试样本仅1例误分类。SVM准确率相近但延迟较高，MLP和GBDT准确率略低，RF略逊于DT。
表1-3多分类器性能对比（多目标优化选优）
模型	准确率	F1-score	交叉验证均值	综合评分	推理延迟	选型结果
决策树(DT)	99.93%	0.9993	99.97%	0.969	~0ms	最优
随机森林(RF)	99.86%	0.9986	99.91%	0.958	~0.02ms	备选
SVM(RBF)	99.89%	0.9989	99.92%	0.954	~1.5ms	延迟受限
GBDT	99.72%	0.9972	99.81%	0.941	~0.1ms	—
MLP	99.51%	0.9951	99.63%	0.928	~0.05ms	—

基于上述对比结果，最终选定决策树作为生产环境的业务识别模型。其推理开销极低，可在每步对数百架无人机执行识别而不影响仿真效率。此外，决策树模型天然具备可解释性——其特征重要性分析显示丢包率维度（重要性1.0）是区分三类业务的最关键特征，这与直觉相符：控制信令的丢包率（量级）与环境监测的丢包率（量级）相差四个数量级，形成了最显著的区分边界。
2.4.3 自适应识别更新机制
为避免频繁推理和应对概念漂移，设计如下自适应识别更新策略：(1)最小更新间隔5步；(2)概率化触发（基础20%，漂移时40%，置信度<0.8时50%）；(3)漂移检测：维护100长度滑动窗口，最近30次错误率>25%时判定漂移，上调更新概率直至恢复。
2.5增强切换决策算法设计
在深入剖析传统A3事件算法的固有缺陷之后，本章提出一种融合多机制协同的增强切换决策算法`EnhancedHandoverAlgorithm`。该算法的设计理念是在保留传统算法简洁高效优势的同时，从五个维度对其进行系统性增强，使之具备业务感知能力、弹性保障能力和自优化能力。下面依次阐述各核心组件的技术细节。
2.5.1 业务感知的多目标效用函数设计
效用函数整合SINR、负载率和速率匹配度，并根据业务类型动态调整权重。对于一个候选基站和无人机在降级比率下的效用值计算公式为：
		（2-15）
其中各项的含义及计算方式如下：
(1) 归一化SINR项 ： 将原始SINR值（单位dB）以-10dB为零点（SINR低于此值时信号质量极差）、+30dB为满点，经线性变换映射至[0,1]区间。
		（2-16）
(2) 负载空闲度项 ： 直接使用基站负载率的补数，负载越低该项越高，鼓励算法在资源充裕的基站间建立连接。
(3) 速率匹配度： 衡量基站可用容量相对于无人机请求速率的充裕程度，采用饱和指数函数建模：
		（2-17）
	当可用容量等于请求速率时匹配度约为0.95，两倍于请求速率时接近1.0（边际收益递减），不足请求速率时快速下降。这种非线性特性避免了效用函数对微小容量差异的过度敏感。
(4)高速率奖励 ： 当降级比率时给予小额正向激励：
		（2-18）
该奖励项旨在略微偏向全速率或近全速率的分配方案，避免算法过度倾向于保守的降级策略。
(5) 业务特化权重 ： 三组权重的配置取决于无人机被识别出的业务类型，本文实现了两种权重配置方案。"论文参数"（paper）方案下：控制信令业务，强调信号质量的可靠性；视频回传业务，突出带宽匹配的重要性；环境监测业务，将速率匹配作为主导因素。在"调优参数"（optimized）方案（用于MAPPO对比实验以拉开性能差距）下，各权重的分化更加剧烈：控制信令进一步强化SINR的主导地位，视频回传和环境监测均将速率权重提升至0.60。
配合效用函数的动态切换阈值则由多因子联合确定：
		（2-19）
其中基础阈值；业务因子对控制信令取0.5（降低阈值使其更容易触发切换以保障可靠性），其他业务取1.0；负载调整项在当前基站负载率高时为负值（抑制切换），控制信令业务在负载率超过70%时额外加大抑制力度；置信度调整项，识别置信度越低阈值越高（不确定性大时更保守）；移动性调整项，高速移动时降低阈值以更快响应链路变化；优先级调整项对控制信令额外降低0.0045；全局负载自适应项仅在optimized模式下激活，全局负载率高于85%时提高阈值使策略趋于保守，低于70%时降低阈值使策略更为激进。最终的动态阈值还需满足下界约束：控制信令的下界为基础下界的50%，其他业务为100%，确保关键业务始终享有更灵敏的切换响应。
2.5.2 多级回滚与兜底保障机制
为克服传统算法"先断后连"模式的断连风险，增强算法设计了一套三级递进的容错保障机制，在每次切换执行过程中依次尝试多种恢复手段，最大限度地减少断连事件的发生。
第一级：直接分配。切换执行的初始步骤与传统算法一致：先释放旧基站资源，再向目标基站申请分配。若目标基站有足够的可用容量，则分配成功，切换正常完成。此时算法还会根据当前系统状态和无人机业务属性计算本次切换的动态冷却时间，将该无人机标记为冷却期内以防止乒乓切换。冷却时间的计算同样考虑多重因素：基础冷却步数为5步，随后根据业务优先级（高优先级缩短）、系统负载（高负载延长）、当前SINR（信号差时缩短以加速逃离、信号好时延长以避免不必要的切换）以及业务类型（控制信令-2步、视频+1步、环境监测不变）进行动态调整，最终裁剪至[1, 10]步范围内。
第二级：抢占与软迁移。 若直接分配失败（目标基站容量不足），算法启动抢占流程：调用目标基站的`kick_low_priority`方法，按照优先级升序遍历当前连接的低优先级且非关键的无人机，逐个释放其占用的资源直到腾出足够空间或遍历完所有候选。关键业务（）的无人机被排除在抢占对象之外，以确保核心业务不受侵害。抢占成功并获得足够容量后，目标基站重新执行分配操作。与此同时，被抢占的无人机进入软迁移队列——算法为其搜索替代基站（排除刚完成抢占的目标基站），优先选择SINR损失不超过5dB且有足够容量的基站进行重分配，尽最大程度减少"连锁断连"的扩散效应。
第三级：回滚与兜底。 如果抢占仍然未能释放足够空间（例如目标基站上所有已连接用户均为高优先级），算法启动回滚流程：尝试将被释放的旧基站资源重新夺回。回滚首先尝试直接分配（因为旧基站的容量在释放后应当已经恢复），若旧基站也被其他并发抢占耗尽，则对旧基站同样执行抢占操作。如果回滚成功，无人机回到原有连接状态，切换等效于"未发生"；如果回滚也失败，无人机最终进入断连状态并被计入回滚失败计数和幽灵断连计数。对于断连的无人机，算法在下一仿真步会优先处理——按断连持续时间排序，断连越久的无人机越优先尝试重连。
综上所述，增强算法采用"直接分配→抢占软迁移→回滚恢复→断连兜底"的三级保障机制，显著降低断连风险。
2.5.3 探索-贪心策略集成
纯粹的贪心策略可能陷入次优稳态，为此本文引入ε-greedy探索机制。在每次决策中，算法以1%的概率绕过效用函数，从候选基站中均匀随机选取目标（排除当前服务基站），然后依次尝试降级比率1.0、0.8、0.6，执行第一个可行的分配方案。1%的探索率是权衡后的保守取值：过高会增加不必要切换，过低则无法发挥探索价值。
2.5.4 高负载退化机制与全局负载均衡
高负载模式下（全局平均负载率超过85%），算法退化为保守的类A3策略：仅当邻区SINR超过服务小区SINR加3dB（比标准2dB更保守）时触发切换，同时禁用ε-greedy探索、降级搜索、抢占和负载均衡。该机制有效遏制了负载尖峰时刻的切换失败率。此外，算法每隔5步执行周期性全局负载均衡：先计算基站负载率标准差，若小于0.05则跳过；否则定位负载率最高和最低的基站，若二者差距小于0.1则无需调整。从高负载基站筛选满足以下条件的迁移候选：迁移后SINR损失≤5dB、目标基站剩余容量≥30%、迁移后效用≥当前效用的95%。候选按“优先级低且关键度高”优先排序，每轮最多迁移5架无人机，复用完整切换流程。
2.5.5 算法完整流程与复杂度分析
增强切换算法每步执行流程如下：首先处理断连无人机，按断连持续时间降序依次尝试重连。随后对正常连接的无人机依次执行冷却期检查（跳过冷却期内无人机）、高负载模式判定（全局负载率>85%时仅当SINR显著改善才切换）、紧急切换判定（SINR或满意度不足时搜索最优基站）、ε-greedy探索（1%概率随机选择）以及效用比较决策（遍历邻区和五种降级比率，若效用增益超过动态阈值则触发切换）。切换指令按“直接分配→抢占软迁移→回滚→断连兜底”三级保障执行。最后，每5步且非高负载时执行一轮全局负载均衡。
从计算复杂度的角度分析，设为基站数量、为无人机数量、为降级比率数量级数、为单次效用计算的常数时间复杂度。单个无人机的决策过程需遍历个候选方案并各执行一次效用计算，复杂度为；全部无人机的决策总复杂度为。切换执行的抢占操作最坏情况下需遍历目标基站上的所有连接用户，复杂度为。全局负载均衡涉及排序和筛选，复杂度为。总体而言，算法的单步复杂度为级别，对于本文实验中的典型规模（8基站、300~500架无人机），单步决策耗时在毫秒量级，完全满足在线运行的要求。
2.6本章小结
本章围绕系统建模、需求分析、问题诊断、模块设计与算法构建的主线，完成了无人机差异化服务场景的系统建模与业务感知增强切换算法的完整设计。首先，建立了包含网络拓扑、信道传播和容量负载的仿真框架，采用对数距离路径损耗与瑞利衰落刻画空地信道，支持宏/小基站混合部署及多场景配置，并通过SINR驱动的时延/丢包率模型建立了物理层到业务层QoS的定量映射。其次，形式化定义了控制信令、视频回传、环境监测三类业务的QoS需求模型，涵盖速率、时延、丢包率、优先级、关键程度、降级容忍度和时延敏感度等参数，并设计了基于非线性阶梯函数的速率满意度模型和时延敏感度加权综合满意度评估体系，为后续实验提供了统一基准。再次，通过理论与初步实验揭示了3GPP A3算法在无人机场景下的四方面缺陷（资源错配、断连风险、缺乏弹性、阈值固定），明确了改进方向。然后，设计实现了包含特征工程、多分类器选型和自适应更新的业务识别模块，经多目标对比（准确性40%、稳定性30%、实时性30%）选定决策树为最优模型（准确率99.93%，F1=0.9993，延迟趋近零），并提出了基于最小间隔约束、概率化触发和漂移检测的自适应更新机制。最后，提出融合五类协同机制的增强切换算法：业务感知多目标效用函数（整合SINR、负载率、速率匹配度并按业务调整权重）、多因子动态阈值、三级回滚保障链（直接分配→抢占软迁移→回滚恢复→断连兜底）、ε-greedy探索、高负载退化与周期性全局负载均衡。该算法无需离线训练，具备良好的冷启动和跨场景泛化能力。实验表明，相比传统A3基线，增强算法在整体平均满意度、关键业务满意度和连接保持率上分别提升11.03%、5.08%和19.29%，负载方差降低约95.4%。本章构成规则驱动方法论的核心，下一章转向数据驱动的多智能体深度强化学习方法。