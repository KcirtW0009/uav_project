# 第5章 总结与展望

## 5.1 工作总结

本文围绕无人机在5G网络中进行基站间通信切换时面临的业务服务质量保障问题，提出并实现了一套业务识别与切换决策联动系统。该系统以"感知—决策—评估"的三层联动架构为核心框架，通过业务类型识别模块为切换决策提供差异化依据，通过多机制协同的增强切换算法提升切换鲁棒性和资源利用效率，通过层次化满意度评估模型对系统性能进行精细化度量。全文的研究工作按照系统设计、算法实现、实验验证的逻辑链条展开，形成了一个从问题建模到方案验证的完整研究闭环。

在系统总体设计层面，本文构建了一个面向无人机通信场景的网络仿真平台，涵盖了基站与无人机实体建模、3GPP UMa路径损耗与Rayleigh小尺度衰落相结合的无线信道模型、三维随机移动模型以及多类型随机事件注入机制。仿真平台的各项参数参考了5G网络的实际部署标准，基站容量、覆盖半径、信道模型等关键配置均有据可依，保证了仿真结果对真实场景的参考价值。在此基础上，设计了控制信令、视频回传和环境监测三类典型无人机业务的差异化服务质量需求模型，并构建了从速率、时延、丢包率三个维度出发、结合Smoothstep非线性满意度函数的层次化满意度评估体系。该评估体系的一个关键设计在于满意度计算始终基于无人机的真实业务类型而非识别结果，这一机制为准确评估识别误差对系统性能的影响提供了可靠的度量基础。

在业务类型识别模块方面，本文提取了需求速率、时延要求、丢包容忍度和优先级等级四个维度的业务特征，构成四维特征向量，并通过多目标优化评分函数从准确率、F1分数、关键业务召回率和推理延迟四个指标对五种分类算法进行了综合评估，最终选取多层感知机（MLP）作为识别模型。识别模块还设计了自适应更新机制和置信度输出接口，使其能够与切换决策模块实现信息联动。

在切换算法设计层面，本文按照"传统基线→增强启发式→数据驱动"的递进逻辑设计了三种层次化切换算法。基于3GPP A3事件触发机制的传统切换算法忠实复现了标准化切换流程，作为性能对比基线，并系统分析了其在业务感知、资源利用和切换鲁棒性方面的固有局限。增强切换算法针对上述局限进行了七个层面的系统性改进：业务感知效用函数通过差异化权重实现多维度综合评估，动态切换阈值机制根据业务优先级、基站负载、识别置信度和移动性特征自适应调整切换触发条件，降级比例搜索机制通过五档降级和穷举组合扩展资源获取空间，优先级抢占机制保护关键业务不被低优先级用户挤占，回滚保护机制在切换失败时恢复原有连接，软迁移机制在目标基站资源暂时不足时维持旧连接直至新连接就绪，全局负载均衡机制通过周期性迁移优化网络整体资源分配。基于深度强化学习的DQN方法将切换决策建模为马尔可夫决策过程，设计了48维状态空间和9维离散动作空间，并通过容量比值特征、全局资源紧张度指标和自适应切换惩罚等增强设计提升了模型在资源竞争条件下的决策质量。DQN采用单智能体混合管理架构，仅对目标无人机执行强化学习决策，其余无人机仍由增强启发式算法管理，在保证RL探索自由度的同时维持了系统整体运行稳定性。

在实验验证层面，本文设计了五组递进式实验，从模块级验证到系统级对比再到场景泛化，对所提方案进行了全面评估。实验一通过五级识别准确率梯度（100%、90%、80%、60%、33%）验证了业务识别准确性对系统整体服务质量的定量影响，结果表明识别准确率从33%提升至100%可使整体满意度提升28.6%、加权满意度提升38.5%、资源匹配度改善94.4%，为"业务感知"切换决策框架的价值提供了坚实的实证支撑。实验二和实验二b通过单机制消融和多机制协同消融实验，量化了增强算法中各机制的独立贡献和组合效应，发现动态切换阈值是贡献最大的单一机制（切换成功率提升19.4%、负载方差降低92.8%），且四机制全开组合并非总是优于最优三机制子集，揭示了机制间存在策略冲突的可能性。实验三在标准仿真条件下对增强算法与传统算法进行了包含7项核心指标和统计显著性检验的全面对比，结果显示增强算法在切换成功率（+16.2%，p=0.0004）、整体满意度（+9.6%，p=0.008）、负载均衡度（改善94.4%，p<0.001）和总负载（+18.3%，p=0.002）等指标上均实现了具有统计显著性的改善，其中6项指标的差异通过了假设检验。实验四在智慧城市监控、工业巡检、农业监测、应急救援和物流配送五个差异化应用场景中验证了系统的泛化能力，结果表明增强算法在资源受限的农业监测场景中优势最为显著（满意度提升45.6%），在资源充裕场景中切换成功率仍维持100%，且负载均衡能力在所有场景中表现一致。实验五引入DQN强化学习方法，在10至40架无人机的四种密度条件下与两种启发式算法进行了对比，并通过特征消融实验验证了DQN状态空间增强设计的有效性，同时客观分析了DQN方法在训练开销、全局调控和可解释性方面的局限性。

## 5.2 主要创新点

本文的主要创新点可以归纳为以下三个方面。

第一，提出了业务识别与切换决策的三层联动架构，实现了从业务感知到切换执行再到效果评估的闭环工作机制。与现有研究大多将业务识别和切换决策作为独立问题分别讨论不同，本文将两者通过明确的接口进行深度耦合——识别模块输出的业务类型和置信度直接驱动切换决策中的效用函数计算和阈值调整，而满意度评估模块始终基于真实业务类型进行独立评估，使得识别误差的影响能够被准确量化和追踪。这种架构设计不仅提升了系统的整体协调性，也为后续通过提升识别精度来优化切换质量提供了清晰的技术路径。

第二，设计并实现了一套包含七种协同机制的业务感知增强切换算法，在切换鲁棒性和资源利用效率上显著优于传统3GPP A3算法。相较于已有研究中常见的单一维度优化（如仅引入负载感知或仅设计差异化阈值），本文的增强算法从决策和执行两个层面进行了系统性重构。在决策层面，业务感知效用函数综合考虑信号质量、负载状况和速率匹配度三个维度，并根据业务类型自适应调整权重；动态切换阈值机制融合优先级、负载、置信度和移动性四个因子，实现了差异化和场景自适应的切换触发策略。在执行层面，降级比例搜索、优先级抢占、回滚保护和软迁移构成了从资源请求到失败恢复的四级保障链路，全局负载均衡机制从网络整体层面优化资源分配的公平性。实验表明，这七种机制的协同作用使切换成功率提升16.2个百分点，负载均衡度改善94.4%，且在五个差异化应用场景中均表现出一致的优越性。

第三，构建了面向无人机切换决策的DQN强化学习框架，并通过增强状态空间设计和混合管理架构探索了数据驱动方法在该领域的应用路径。本文在DQN状态空间中引入了基站容量比值特征、全局资源紧张度指标和自适应切换惩罚三个增强设计，分别从微观基站级、宏观系统级和策略级三个层面丰富了智能体对环境的感知能力，并通过消融实验验证了每个特征的正向贡献。单智能体混合管理架构的提出解决了纯RL方法中全局调控能力不足的问题——仅对一架目标无人机执行RL决策，其余无人机由增强启发式算法集中管理，在保证RL探索自由度的同时维持了系统的整体运行稳定性。实验表明，DQN方法在多种无人机密度条件下达到了与增强启发式算法相当的性能水平，且展现出了"保守切换"等自主发现的优化策略，为数据驱动方法在无人机切换决策中的应用提供了有价值的经验积累。

## 5.3 不足之处

尽管本文所提出的系统在实验验证中展现出了良好的性能表现，但客观分析仍存在以下几方面的不足。

在仿真平台层面，当前的网络仿真环境采用离散时间步进的确定性模型，每个时间步内所有无人机同时执行移动和切换决策。这种同步仿真方式虽然简化了实现复杂度，但未能精确反映实际网络中异步事件驱动的运行特征——现实中无人机的移动和切换请求在时间上是连续且异步发生的，不同无人机之间的切换操作可能存在复杂的时序依赖关系。此外，仿真环境中的信道模型采用了简化的路径损耗加Rayleigh衰落的组合模型，虽然参考了3GPP标准中的UMa参数配置，但未考虑无人机特有的空气到地面（A2G）信道的时变特性，如多径效应的角度依赖性和大气折射等影响因素。

在识别模块层面，当前的业务类型识别基于需求速率、时延要求、丢包容忍度和优先级等级四个静态特征进行分类。这些特征本质上是对业务QoS需求的先验描述，而非从实际通信流中实时提取的统计特征。在实际网络部署中，业务类型信息通常不会以显式字段的形式随数据包传输，而是需要通过深度包检测（DPI）或流量统计分析等手段从加密或未加密的数据流中推断获取。因此，当前的识别方案在实际部署时需要额外的流量特征提取模块作为前置处理，这将引入额外的计算开销和潜在的识别延迟。

在切换算法层面，增强算法中的若干参数（如效用函数的权重系数、动态阈值的基准值和调节因子、降级搜索的档位划分等）目前采用人工设定的固定值。虽然这些参数的选取参考了3GPP标准和相关文献中的推荐配置，并通过实验验证了其合理性，但固定的参数配置难以适应网络拓扑和业务分布的动态变化。例如，当网络中控制信令类业务的占比从默认的40%变化至80%时，当前的权重配置可能不再是最优选择。此外，全局负载均衡机制的迁移粒度和执行频率尚未经过系统化的参数敏感性分析，在特定的时变负载条件下可能触发不必要的迁移操作，反而影响系统稳定性。

在强化学习方法层面，DQN当前的实现存在若干局限性。单智能体架构限制了DQN对全局资源分配的优化能力，在资源严重不足的极高密度场景中，仅控制一架无人机的切换决策难以对系统整体性能产生实质性影响。DQN的训练需要1000个episode的环境交互，在当前仿真条件下训练时间尚可接受，但在更复杂的仿真环境（如更多基站、更多无人机、更长的仿真周期）或实际网络中，训练开销可能成为部署瓶颈。此外，DQN的决策过程缺乏可解释性，网络权重的具体含义难以用工程语言表达，这在需要合规审查和故障诊断的电信网络运维场景中可能构成障碍。

在实验设计层面，当前的场景配置虽然覆盖了五种典型的无人机应用领域，但各场景的业务分布和参数配置仍基于简化假设。实际应用中的业务需求可能更加复杂和动态，例如应急救援场景中可能同时存在视频回传（灾区实时画面）和控制信令（飞行控制指令）等多种业务类型，且其优先级可能随任务阶段动态变化。此外，当前实验未考虑无人机之间的相互干扰、基站故障恢复的时序过程以及多无人机协同任务中的编队通信需求等实际因素。

## 5.4 未来工作展望

基于上述分析，本文认为以下方向值得在未来工作中进一步深入探索。

在仿真平台完善方面，可考虑引入事件驱动的异步仿真机制，以更精确地模拟实际网络中事件发生的时序特性。信道模型可进一步集成3GPP TR 36.777中推荐的无人机专用信道模型，纳入高度依赖的路径损耗修正因子和多径分量角度分布等参数，提升仿真对真实A2G信道的拟合度。此外，可引入更丰富的随机事件模型，如基站容量动态波动、无人机任务切换导致的业务类型变更、突发的大规模无人机群涌入等场景，以检验系统在更复杂动态条件下的鲁棒性。

在识别模块改进方面，未来可将识别特征从静态QoS描述扩展至实时流量统计特征，如包到达间隔的分布参数、突发流量的持续时间特征、上下行流量比值等，使识别模块能够直接从通信流中推断业务类型而无需依赖先验描述。在识别模型方面，可探索轻量化的在线学习方案，利用概念漂移检测技术（如ADWIN或DDM算法）实时监测业务特征分布的变化，并在检测到分布偏移时触发模型的增量更新，以适应业务类型随时间演化的动态场景。

在切换算法优化方面，参数自适应是一个值得深入研究的方向。可引入基于在线学习的参数调整机制，根据系统运行过程中收集的性能统计信息（如各基站的切换请求频率、成功率和满意度变化趋势等）动态调整效用函数权重和切换阈值参数，实现算法参数与网络状态的协同演化。在多机制协同层面，可引入机制间的优先级仲裁和互斥逻辑，根据当前网络状态智能地选择启用或禁用特定机制，避免前述实验中观察到的"负协同"现象。此外，可探索基于图神经网络（GNN）的切换决策方法，将基站拓扑关系和无人机空间分布建模为图结构，利用图卷积操作捕捉网络空间结构信息对切换决策的影响。

在强化学习方法演进方面，多智能体强化学习（MARL）是突破当前单智能体架构局限性的自然方向。通过为每架无人机配置独立的强化学习智能体，并引入通信机制使智能体之间能够交换局部观测信息，可以在分布式决策框架下实现接近集中式最优的全局资源分配效果。在算法层面，可探索比DQN更适合连续状态空间的算法，如近端策略优化（PPO）或软演员-评论家（SAC），以处理更复杂的网络状态表示。此外，离线强化学习（Offline RL）和迁移学习（Transfer Learning）技术可以分别解决训练数据获取困难和跨场景泛化能力不足的问题，使训练好的RL模型能够更快速地适应新的部署环境。

在系统实现与部署方面，未来可基于本文的仿真研究成果，进一步开发原型验证系统。可考虑利用软件定义网络（SDN）和网络功能虚拟化（NFV）技术，将业务识别模块和切换决策模块实现为可编程的网络功能组件，部署在实际的5G试验网络中进行验证。在标准化层面，本文提出的业务感知切换机制和差异化QoS保障策略可为3GPP R18及后续版本中关于无人机通信增强（UAV UE enhancements for NR）的标准化讨论提供技术参考。此外，可将本文的研究框架向6G网络中的场景进行前瞻性延伸，探索通信感知一体化（ISAC）、空天地一体化网络架构和原生人工智能（Native AI）等新兴技术对无人机切换决策的影响和机遇。

综上所述，本文所提出的无人机业务识别与切换决策联动系统在仿真实验中展现出了良好的性能表现和场景适应性，为无人机通信服务质量保障问题提供了一套完整的技术方案。虽然当前研究在仿真保真度、算法自适应性和工程实用性方面仍存在提升空间，但本文所建立的系统架构、算法设计和实验方法论为后续的深入研究奠定了坚实的基础。随着5G网络的持续演进和无人机应用的不断拓展，基于业务感知的智能化切换决策必将在未来的空天地一体化通信网络中发挥越来越重要的作用。
