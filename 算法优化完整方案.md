# 算法优化完整方案

## 设计愿景

### 核心目标

1. **决策阶段稳稳地筛选**
   - 决策阶段就过滤掉成功率低的基站
   - 执行阶段**不再过滤**，直接执行

2. **合理的切换成功率**
   - 进入执行阶段的切换允许约5%的失败
   - 切换成功率 ≈ 95%左右（真实）

3. **适当的预测模型放松**
   - 避免预测过于保守
   - 减少兜底机制的触发频率

---

## 当前问题诊断

### 问题1: 决策阶段兜底过于频繁

**统计**:
```
有候选的决策: 3513次
├─ 第一层（≥0.55）: 1858次 (52.9%)
├─ 第二层（≥0.4但<0.55）: 917次 (26.1%)  ← 兜底
└─ 第三层（全部）: 738次 (21.0%)           ← 兜底

兜底使用率: 47.1%
```

**原因**: 预测模型过于保守
```python
load_success = np.exp(-3.0 * bs.load_ratio)
```
负载85%时：预测仅0.078（即使SINR很高）

### 问题2: 执行阶段过滤导致统计偏差

**统计**:
```
进入execute: 1544次
├─ 预测≥0.55（计入）: 686次 (44.4%)
└─ 预测<0.55（过滤）: 858次 (55.6%)

当前成功率 = 686/686 = 100%
真实成功率 = 686/1544 = 44.43%
```

### 问题3: 决策与执行逻辑冲突

```
决策: 没有高预测候选 → 兜底选择低预测 → 返回
                                   ↓
执行: 预测低 → 不执行 → 兜底毫无意义
```

---

## 优化方案：决策阶段统一筛选

### 核心设计原则

1. **决策阶段：统一预测阈值**
   - 不再分三层兜底（0.55, 0.4, 全部）
   - 使用**单一阈值**，但适当放宽（如0.45-0.50）

2. **执行阶段：移除统计过滤**
   - 所有进入execute的都计入`handover_attempts`
   - 不再有"不计入"的切换

3. **预测模型：适当放松**
   - 降低负载影响系数（-3.0 → -2.0）
   - 提高预测成功率，减少兜底触发

---

## 详细修改方案

### 修改1: 预测模型放松

**文件**: `uav_system/algorithms.py`
**位置**: `predict_handover_success` 方法，line 232-256

**当前代码**:
```python
def predict_handover_success(self, uav, bs_id: int, downgrade_ratio: float) -> float:
    sinr = self.env.sinr_matrix[uav.uav_id, bs_id]
    bs = self.env.base_stations[bs_id]

    sinr_success = 1 / (1 + np.exp(-0.5 * (sinr + 5)))

    # 修复：使用更保守的负载预测模型
    # 原公式：load_success = 1 - load_ratio * 0.5，负载80%时预测60%
    # 新公式：使用指数衰减，负载80%时预测降至约20%
    load_success = np.exp(-3.0 * bs.load_ratio)  # ← 过于保守

    # 实际可用容量检查
    required_rate = uav.required_rate * downgrade_ratio
    if bs.available_capacity < required_rate * 0.8:
        load_success = min(load_success, 0.1)

    # ...
    success_prob = (business_weight * sinr_success +
                    (1 - business_weight) * load_success) * confidence_factor
    return np.clip(success_prob, 0, 1)
```

**修改后**:
```python
def predict_handover_success(self, uav, bs_id: int, downgrade_ratio: float) -> float:
    sinr = self.env.sinr_matrix[uav.uav_id, bs_id]
    bs = self.env.base_stations[bs_id]

    sinr_success = 1 / (1 + np.exp(-0.5 * (sinr + 5)))

    # 优化：放松负载预测模型，提高预测成功率
    # 原系数：-3.0 → 负载80%时预测：exp(-2.4) = 0.090
    # 新系数：-2.0 → 负载80%时预测：exp(-1.6) = 0.202 (提升124%)
    load_success = np.exp(-2.0 * bs.load_ratio)

    # 实际可用容量检查（保持不变）
    required_rate = uav.required_rate * downgrade_ratio
    if bs.available_capacity < required_rate * 0.8:
        load_success = min(load_success, 0.1)

    # 业务权重（保持不变）
    if uav.business_type == BusinessType.CONTROL_SIGNAL:
        business_weight = 0.7
    elif uav.business_type == BusinessType.VIDEO_STREAMING:
        business_weight = 0.5
    else:
        business_weight = 0.3

    confidence_factor = 0.5 + 0.5 * uav.recognition_confidence
    success_prob = (business_weight * sinr_success +
                    (1 - business_weight) * load_success) * confidence_factor
    return np.clip(success_prob, 0, 1)
```

**预期效果**:
| 负载 | 原预测(-3.0) | 新预测(-2.0) | 提升 |
|-------|----------------|----------------|------|
| 60%   | 0.165          | 0.301          | +82% |
| 70%   | 0.123          | 0.247          | +101% |
| 80%   | 0.090          | 0.202          | +124% |
| 85%   | 0.078          | 0.174          | +123% |
| 90%   | 0.067          | 0.165          | +146% |

---

### 修改2: 决策阶段统一预测阈值

**文件**: `uav_system/algorithms.py`
**位置**: `make_intelligent_decision` 方法，line 326-330

**当前代码**:
```python
# 构建候选
high_success_candidates = [c for c in candidates if c[3] >= 0.55]  # 第一层
if not high_success_candidates:
    high_success_candidates = [c for c in candidates if c[3] >= 0.4]  # 第二层兜底
if not high_success_candidates:
    high_success_candidates = candidates  # 第三层兜底（全部）
```

**问题**:
- 47.1%的决策使用兜底
- 三层阈值不统一

**修改后**:
```python
# 构建候选 - 使用统一阈值
UNIFIED_THRESHOLD = 0.48  # 统一预测阈值（放宽至0.48）

high_success_candidates = [c for c in candidates if c[3] >= UNIFIED_THRESHOLD]

# 如果仍然没有候选（极端情况），才使用兜底
if not high_success_candidates:
    # 兜底策略：选择预测最高的候选（但预测都很低）
    high_success_candidates = sorted(candidates, key=lambda x: x[3], reverse=True)
    # 只取前3个最优候选
    high_success_candidates = high_success_candidates[:3]
```

**关键改进**:
1. **统一阈值**: 不再分层，使用单一阈值0.48
2. **适度放宽**: 从0.55降至0.48（降低12.7%）
3. **兜底优化**: 只有在极端情况下才兜底，且只选最优3个

**预期效果**:
```
预测模型放松 + 阈值放宽
├─ 预测成功率整体提升
├─ 更多的候选达到0.48阈值
├─ 兜底使用率从47.1%降至约15-20%
└─ 决策质量提升
```

---

### 修改3: 执行阶段移除统计过滤

**文件**: `uav_system/algorithms.py`
**位置**: `execute_handover` 方法，line 418-480

**当前代码**:
```python
def execute_handover(self, uav_id: int, target_bs_id: int, downgrade_ratio: float) -> bool:
    from time import time
    t_start = time()

    uav = self.env.uavs[uav_id]

    # 计算预测成功率，只有预测成功率≥0.55时才计入正式尝试
    success_prob = self.predict_handover_success(uav, target_bs_id, downgrade_ratio)
    count_as_attempt = success_prob >= 0.55  # 是否计入正式尝试
    if count_as_attempt:
        self.handover_attempts += 1
    else:
        # 低于阈值的切换不计入尝试，避免降低成功率
        self.execution_filter_stats['below_threshold'] = self.execution_filter_stats.get('below_threshold', 0) + 1
        t_end = time()
        return False

    # ... 执行切换
```

**问题**:
- 执行阶段再次预测
- 低于阈值不计入统计（统计偏差）
- 与决策阶段冲突

**修改后**:
```python
def execute_handover(self, uav_id: int, target_bs_id: int, downgrade_ratio: float) -> bool:
    from time import time
    t_start = time()

    uav = self.env.uavs[uav_id]
    target_bs = self.env.base_stations[target_bs_id]
    required_rate = uav.required_rate * downgrade_ratio

    # 所有调用都计入尝试（移除统计过滤）
    self.handover_attempts += 1

    # 记录旧基站信息以便回滚
    old_bs_id = uav.connected_bs_id
    old_bs = self.env.base_stations[old_bs_id] if old_bs_id is not None else None
    old_allocated_rate = uav.current_allocated_rate

    # 先尝试分配到新基站（先分配后释放）
    if target_bs.allocate(uav_id, required_rate):
        # 分配成功，再释放旧连接
        if old_bs_id is not None and old_bs_id != target_bs_id:
            old_bs.release(uav_id)
            self.env.connection_matrix[uav_id, old_bs_id] = 0

        uav.connected_bs_id = target_bs_id
        uav.current_allocated_rate = required_rate
        self.env.connection_matrix[uav_id, target_bs_id] = 1
        uav.handover_count += 1
        self.handover_successes += 1

        t_end = time()
        self.switching_latency_history.append((t_end - t_start) * 1000)
        return True

    else:
        self.failure_reasons['allocation_failed'] += 1

        # 尝试抢占低优先级UAV
        freed = target_bs.kick_low_priority(uav, self.env.uavs)
        if freed >= required_rate and target_bs.allocate(uav_id, required_rate):
            # 抢占成功，再释放旧连接
            if old_bs_id is not None and old_bs_id != target_bs_id:
                old_bs.release(uav_id)
                self.env.connection_matrix[uav_id, old_bs_id] = 0

            uav.connected_bs_id = target_bs_id
            uav.current_allocated_rate = required_rate
            self.env.connection_matrix[uav_id, target_bs_id] = 1
            uav.handover_count += 1
            self.handover_successes += 1

            t_end = time()
            self.switching_latency_history.append((t_end - t_start) * 1000)
            return True

        # 抢占失败，UAV保持原连接
        self.failure_reasons['preemption_failed'] += 1

        t_end = time()
        self.switching_latency_history.append((t_end - t_start) * 1000)
        return False
```

**关键改进**:
1. **移除预测过滤**: 不再重新计算预测
2. **所有都计入统计**: `handover_attempts += 1` 在函数开始
3. **逻辑统一**: 执行阶段只负责执行，不负责筛选

**预期效果**:
```
执行阶段:
├─ 所有切换都计入统计 ✓
├─ 切换成功率反映真实性能 ✓
└─ 与决策阶段逻辑一致 ✓
```

---

### 修改4: 调整决策阶段的多层过滤

**文件**: `uav_system/algorithms.py`
**位置**: `make_intelligent_decision` 方法，line 378-413

**当前代码**:
```python
# 过滤条件1：当前基站负载较低时不切换（避免不必要的切换）
if current_bs.load_ratio < 0.3:
    if best_success_prob < 0.55:
        self.missed_opportunity += 1
        return None

# 过滤条件2：目标基站历史成功率较低时谨慎切换
if best_success_prob < 0.55 and uav.current_satisfaction >= 0.7:
    self.missed_opportunity += 1
    return None

# 过滤条件3：连接稳定性检查
if sinr_gain < 2.0 and uav.current_satisfaction >= 0.8 and best_success_prob < 0.7:
    self.missed_opportunity += 1
    return None
```

**问题**:
- 使用硬编码阈值（0.55, 0.7）
- 与统一决策阈值（0.48）不匹配

**修改后**:
```python
# 定义统一阈值常量
DECISION_THRESHOLD = 0.48  # 决策阶段的统一预测阈值
CONSERVATIVE_THRESHOLD = 0.55  # 保守阈值（用于稳定性检查）
HIGH_SUCCESS_THRESHOLD = 0.70  # 高成功率阈值

# 过滤条件1：当前基站负载较低时不切换（避免不必要的切换）
if current_bs is not None and current_bs.load_ratio < 0.3:
    # 放宽阈值，与统一决策阈值保持一致
    if best_success_prob < DECISION_THRESHOLD:
        self.decision_log[-1]['filter_reason'] = 'low_load'
        self.decision_log[-1]['load_threshold'] = 0.3
        self.missed_opportunity += 1
        return None

# 过滤条件2：目标基站预测成功率较低时谨慎切换
# 适当放宽阈值，从0.55降至0.48
if best_success_prob < DECISION_THRESHOLD and uav.current_satisfaction >= 0.7:
    self.decision_log[-1]['filter_reason'] = 'low_success_prob'
    self.decision_log[-1]['prob_threshold'] = DECISION_THRESHOLD
    self.missed_opportunity += 1
    return None

# 过滤条件3：连接稳定性检查
# 保持较高阈值，确保连接稳定性
if current_bs is not None and current_bs.load_ratio < 0.7:
    current_sinr = self.env.sinr_matrix[uav_id, current_bs_id]
    target_sinr = self.env.sinr_matrix[uav_id, best_bs]
    sinr_gain = target_sinr - current_sinr

    # 如果SINR提升很小且当前满足率较高，过滤切换
    if sinr_gain < 2.0 and uav.current_satisfaction >= 0.8 and best_success_prob < HIGH_SUCCESS_THRESHOLD:
        self.decision_log[-1]['filter_reason'] = 'low_gain'
        self.decision_log[-1]['sinr_gain'] = sinr_gain
        self.missed_opportunity += 1
        return None
```

**关键改进**:
1. **定义常量**: 统一管理阈值，便于调参
2. **阈值对齐**: 过滤条件使用DECISION_THRESHOLD(0.48)，与候选筛选一致
3. **保持稳定性**: 条件3使用HIGH_SUCCESS_THRESHOLD(0.70)，确保连接稳定

---

## 预期效果评估

### 修改前 vs 修改后对比

| 指标 | 修改前 | 修改后（预期） | 改进 |
|-----|--------|----------------|------|
| **预测模型系数** | -3.0 | -2.0 | 放松33% |
| **决策阈值** | 三层(0.55/0.4/全部) | 统一0.48 | 简化 |
| **兜底使用率** | 47.1% | 约15-20% | 降低60-70% |
| **执行阶段过滤** | 55.6%被过滤 | 0% | 移除 |
| **切换成功率** | 100%（虚假） | 约93-96% | 真实 |
| **切换成功率偏差** | 55.57% | <5% | 消除 |

### 详细预测

#### 1. 预测成功率提升

**假设**: 负载分布不变（平均89.2%）

| 候选区间 | 修改前占比 | 修改前平均预测 | 修改后预测 | 预期占比 |
|---------|----------|--------------|------------|---------|
| [0.70, 1.00) | 49.9% | 0.85 | 0.89 | 55-60% |
| [0.55, 0.70) | 0.3% | 0.62 | 0.68 | 5-8% |
| [0.48, 0.55) | 30.5% | 0.50 | 0.54 | 25-30% |
| [0.40, 0.48) | 19.3% | 0.44 | 0.50 | 10-15% |
| [0.00, 0.40) | 0.0% | - | 0.38 | <2% |

**结论**:
- 预测成功率从0.593提升至约0.63-0.65
- 达到0.48阈值的候选增加15-20%

#### 2. 兜底使用率下降

```
修改前: 47.1%（26.1%第二层 + 21.0%第三层）

修改后预期:
├─ 达到0.48阈值: 约80-85%
├─ 需要兜底: 约15-20%
└─ 下降幅度: 60-70%
```

#### 3. 切换成功率（真实）

**假设**:
- 进入execute的切换: 约2000-2500次（因决策更积极）
- 执行失败率: 约3-5%（网络不确定性）

```
切换成功率 = (100% - 失败率) = 95-97%
```

**与传统算法对比**:
```
传统算法: 96.6%
增强算法（修改后）: 95-97%
差异: 仍在合理范围内
```

---

## 实施步骤

### 步骤1: 备份代码

```bash
# 备份原始文件
cp uav_system/algorithms.py uav_system/algorithms.py.backup
```

### 步骤2: 修改预测模型

修改 `predict_handover_success` 方法（line 232-256）

```python
# 原来
load_success = np.exp(-3.0 * bs.load_ratio)

# 改为
load_success = np.exp(-2.0 * bs.load_ratio)
```

### 步骤3: 修改决策阈值

修改 `make_intelligent_decision` 方法（line 326-330）

```python
# 在方法开头添加常量
UNIFIED_THRESHOLD = 0.48

# 替换原来的三层兜底
high_success_candidates = [c for c in candidates if c[3] >= UNIFIED_THRESHOLD]
if not high_success_candidates:
    high_success_candidates = sorted(candidates, key=lambda x: x[3], reverse=True)[:3]
```

### 步骤4: 修改执行阶段

修改 `execute_handover` 方法（line 418-480）

```python
# 移除预测过滤和统计过滤
# 在函数开始直接加入
self.handover_attempts += 1
```

### 步骤5: 调整过滤条件

修改 `make_intelligent_decision` 方法（line 378-413）

```python
# 定义常量
DECISION_THRESHOLD = 0.48
CONSERVATIVE_THRESHOLD = 0.55
HIGH_SUCCESS_THRESHOLD = 0.70

# 更新过滤条件中的阈值
```

### 步骤6: 测试验证

```bash
# 运行测试脚本
python test_execution_filter.py

# 查看关键指标：
# 1. 兜底使用率（目标: <20%）
# 2. 切换成功率（目标: 93-96%）
# 3. 统计偏差（目标: <5%）
```

### 步骤7: 运行完整实验

```bash
# 运行实验3（关键验证）
python main.py 3

# 对比结果
# 切换成功率
# 整体满足率
# 关键业务满足率
```

---

## 参数调优建议

### 如果兜底使用率仍然过高

**调整方向**: 进一步放松预测模型或阈值

```python
# 方案A: 进一步降低系数
load_success = np.exp(-1.8 * bs.load_ratio)  # 从-2.0降至-1.8

# 方案B: 进一步降低阈值
UNIFIED_THRESHOLD = 0.45  # 从0.48降至0.45
```

### 如果切换成功率过低（<90%）

**调整方向**: 提高阈值，更保守

```python
# 方案A: 提高阈值
UNIFIED_THRESHOLD = 0.50  # 从0.48提升至0.50

# 方案B: 恢复预测系数
load_success = np.exp(-2.2 * bs.load_ratio)  # 从-2.0提升至-2.2
```

### 如果满足率显著下降

**调整方向**: 平衡切换频率和资源利用

```python
# 可能需要调整动态阈值计算
def calculate_dynamic_threshold(self, uav):
    # 降低阈值，增加切换频率
    base_threshold = self.base_threshold * 0.8  # 从1.0降至0.8
    ...
```

---

## 总结

### 核心改进

1. **预测模型放松**: -3.0 → -2.0，提升预测成功率124%
2. **决策统一**: 三层兜底 → 统一0.48阈值
3. **执行简化**: 移除统计过滤，所有都计入
4. **阈值对齐**: 过滤条件使用统一阈值0.48

### 预期效果

| 指标 | 目标 |
|-----|------|
| 兜底使用率 | <20% |
| 切换成功率 | 93-96% |
| 统计偏差 | <5% |
| 满足率 | 保持或提升 |

### 设计原则

- **决策阶段**: 稳稳地筛选，统一阈值，适当放松
- **执行阶段**: 直接执行，真实统计，不再过滤
- **整体目标**: 切换成功率约95%，真实反映性能

这个方案符合你的愿景吗？
