# 切换成功率计数机制完整分析

## 一、计数变量定义

### EnhancedHandoverAlgorithm 的关键计数器

```python
self.decision_calls = 0              # 决策调用次数
self.handover_attempts = 0           # 切换尝试次数（分母）
self.handover_successes = 0          # 切换成功次数（分子）
self.missed_opportunity = 0          # 错失机会次数
self.execution_filter_stats = {}     # 执行阶段过滤统计
```

### 切换成功率计算

```python
# algorithms.py line 545
'handover_success_rate': self.handover_successes / max(self.handover_attempts, 1)
```

**关键公式**：
```
切换成功率 = handover_successes / handover_attempts
```

## 二、完整流程与计数阶段

### 流程图

```
开始
  ↓
【阶段0：run_step调用】
  for each UAV:
    decision = make_intelligent_decision(uav_id)
    if decision is not None:
      execute_handover(uav_id, target, ratio)
  ↓
【阶段1：决策阶段 - make_intelligent_decision】
  1. decision_calls += 1
  2. 如果是emergency → 执行_emergency_select并返回
  3. 构建candidates（所有候选基站）
     for each candidate_bs:
       success_prob = predict_handover_success(...)
       candidates.append((bs_id, ratio, utility, success_prob))
  ↓
【阶段1.1：候选筛选】
  high_success_candidates = [c for c in candidates if c[3] >= 0.55]
  if not:
    high_success_candidates = [c for c in candidates if c[3] >= 0.4]
  if not:
    high_success_candidates = candidates  # 兜底：可能包含低预测值
  ↓
【阶段1.2：阈值比较】
  if best_utility <= current_utility + dynamic_threshold:
    return None  # 不切换
  ↓
【阶段1.3：多层过滤】
  过滤条件1: current_bs.load_ratio < 0.3 and best_success_prob < 0.55
    → return None
    → missed_opportunity += 1
    → 记录 filter_reason='low_load'

  过滤条件2: best_success_prob < 0.55 and current_satisfaction >= 0.7
    → return None
    → missed_opportunity += 1
    → 记录 filter_reason='low_success_prob'

  过滤条件3: sinr_gain < 2.0 and current_satisfaction >= 0.8 and best_success_prob < 0.7
    → return None
    → missed_opportunity += 1
    → 记录 filter_reason='low_gain'
  ↓
【阶段1.4：决策返回】
  return (best_bs, best_ratio)  # 返回目标基站和降级比例
  ↓
【阶段2：执行阶段 - execute_handover】
  1. 重新计算预测成功率（注意：可能不同！）
     success_prob = predict_handover_success(uav, target_bs_id, downgrade_ratio)
     count_as_attempt = success_prob >= 0.55
  ↓
【阶段2.1：执行前过滤】
  if count_as_attempt:
    handover_attempts += 1  # 计入尝试（分母）
  else:
    execution_filter_stats['below_threshold'] += 1
    return False  # 不执行切换
  ↓
【阶段2.2：尝试分配】
  if target_bs.allocate(uav_id, required_rate):
    → 分配成功
    → if count_as_attempt:
         handover_successes += 1  # 计入成功（分子）
    → return True
  else:
    → 分配失败，尝试抢占
    → if 抢占成功:
         if count_as_attempt:
           handover_successes += 1
         return True
       else:
         return False（但仍已计入handover_attempts）
```

## 三、各阶段计数详解

### 1. decision_calls（决策调用次数）

**触发位置**：
```python
# line 278
def make_intelligent_decision(self, uav_id: int):
    self.decision_calls += 1
    ...
```

**含义**：
- 每次对某个UAV调用决策函数，计数+1
- 这是**最上层的调用计数**

**数量级**：
```
decision_calls = num_uav × num_steps
```

例如：220台UAV，150步 → `decision_calls ≈ 33,000`

### 2. missed_opportunity（错失机会次数）

**触发位置**：
```python
# line 388, 398, 412 - 决策阶段的三层过滤
if current_bs.load_ratio < 0.3 and best_success_prob < 0.55:
    self.missed_opportunity += 1
    return None

if best_success_prob < 0.55 and current_satisfaction >= 0.7:
    self.missed_opportunity += 1
    return None

if sinr_gain < 2.0 and ... and best_success_prob < 0.7:
    self.missed_opportunity += 1
    return None
```

**含义**：
- 决策阶段**主动放弃**的切换次数
- 因为当前状态良好、或目标预测成功率低、或增益太小

**数量级**：
- 取决于过滤条件的严格程度
- 通常 `missed_opportunity < decision_calls`

### 3. handover_attempts（切换尝试次数 - 分母）

**触发位置**：
```python
# line 428 - 执行阶段
def execute_handover(self, uav_id: int, target_bs_id: int, downgrade_ratio: float):
    success_prob = self.predict_handover_success(uav, target_bs_id, downgrade_ratio)
    count_as_attempt = success_prob >= 0.55

    if count_as_attempt:
        self.handover_attempts += 1
    else:
        # 关键：低于阈值不计入尝试
        self.execution_filter_stats['below_threshold'] += 1
        return False
```

**含义**：
- **只有预测成功率 ≥ 0.55 的切换才计入尝试**
- 这是**关键的分母**

**数量级**：
```
handover_attempts ≈ (decision_calls - missed_opportunity - 无候选返回) × 预测≥0.55的比例
```

### 4. execution_filter_stats['below_threshold']（执行阶段过滤）

**触发位置**：
```python
# line 431
if not count_as_attempt:
    self.execution_filter_stats['below_threshold'] += 1
    return False
```

**含义**：
- 决策阶段已通过，但执行阶段预测 < 0.55
- **不计入handover_attempts**
- **不执行切换**

**为什么会出现这种情况？**

可能原因：
1. **时间差导致状态变化**
   - 决策阶段：`predict_handover_success` 返回 0.60（通过）
   - 执行阶段：基站负载增加，预测降至 0.40（被过滤）

2. **决策阶段使用了兜底候选**
   ```python
   # line 330
   high_success_candidates = candidates  # 可能包含 < 0.4 的候选
   ```
   如果所有候选预测都 < 0.4，仍会选择其中一个传给执行阶段

3. **负载均衡迁移绕过决策阶段**
   ```python
   # global_load_balancing_v2 line 520
   if self.execute_handover(uav_id, low_bs_id, 1.0):
       migrations += 1
   ```
   这个调用不经过`make_intelligent_decision`，可能传入任意基站

### 5. handover_successes（切换成功次数 - 分子）

**触发位置**：
```python
# line 453, 471 - 分配成功或抢占成功
if count_as_attempt:
    self.handover_successes += 1
```

**含义**：
- 只有当`count_as_attempt = True`且切换实际成功时，才计入分子

**注意**：
```python
# 如果 count_as_attempt = False：
# - 切换成功 → handover_successes 不增加
# - 切换失败 → handover_successes 不增加
# - 根本不执行切换
```

## 四、切换成功率的定义

### 当前定义（有问题？）

```python
切换成功率 = handover_successes / handover_attempts
           = (count_as_attempt=True 且 成功的次数) / (count_as_attempt=True 的次数)
```

**关键问题**：
- **分母只包含预测≥0.55的切换**
- **预测<0.55的切换不计入分母**
- 因此成功率会"被人为拉高"

### 分子分母的构成

```
┌─────────────────────────────────────────────┐
│ decision_calls (决策调用次数，总数)           │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 1. emergency返回 → (不计入handover_attempts)│
│ 2. 无候选返回   → (不计入handover_attempts)│
│ 3. 阈值比较失败 → (不计入handover_attempts)│
│ 4. 三层过滤    → (不计入handover_attempts)  │
│    → missed_opportunity += 1               │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 决策阶段通过，返回 (best_bs, best_ratio)    │
└─────────────────────────────────────────────┘
                    ↓
            【进入execute_handover】
                    ↓
┌─────────────────────────────────────────────┐
│ 重新计算预测成功率                           │
│ count_as_attempt = (success_prob >= 0.55)  │
└─────────────────────────────────────────────┘
                    ↓
           ┌────────┴────────┐
           ↓                 ↓
    ┌──────────┐      ┌──────────┐
    │ False    │      │ True     │
    └──────────┘      └──────────┘
         ↓                  ↓
    execution_filter_stats   handover_attempts += 1
    ['below_threshold']    （分母）
         ↓
    return False
    （不执行切换）
                    ↓
           ┌──────────────────┐
           │ count_as_attempt│
           │     = True      │
           └──────────────────┘
                    ↓
           ┌────────┴────────┐
           ↓                 ↓
    ┌──────────┐      ┌──────────┐
    │ 成功     │      │ 失败     │
    └──────────┘      └──────────┘
         ↓                  ↓
    handover_successes += 1  (分子)
```

## 五、关键问题：为什么执行阶段要"二次过滤"？

### 设计意图

从代码注释看：
```python
# line 424
# 计算预测成功率，只有预测成功率≥0.55时才计入正式尝试
```

**可能的原因**：

1. **时间差导致状态变化**
   - 决策阶段：基站A负载30%，预测成功率0.60
   - 执行阶段：其他UAV已切换到A，负载升至90%，预测成功率0.20
   - 执行阶段重新计算，发现预测低，避免失败切换

2. **决策阶段的兜底机制**
   ```python
   # line 326-330
   high_success_candidates = [c for c in candidates if c[3] >= 0.55]
   if not high_success_candidates:
       high_success_candidates = [c for c in candidates if c[3] >= 0.4]
   if not high_success_candidates:
       high_success_candidates = candidates  # 可能 < 0.4
   ```
   如果所有候选预测都很低，仍会选一个传给执行阶段

3. **负载均衡绕过决策**
   ```python
   # global_load_balancing_v2
   if self.execute_handover(uav_id, low_bs_id, 1.0):
   ```
   这个调用不经过决策阶段

### 重复计算预测的问题

**问题**：决策阶段和执行阶段都调用`predict_handover_success`

```python
# 决策阶段 line 320
success_prob = self.predict_handover_success(uav, bs_id, ratio)

# 执行阶段 line 425
success_prob = self.predict_handover_success(uav, target_bs_id, downgrade_ratio)
```

**这两次计算可能不同**，因为：
- 时间流逝，UAV位置变化
- 基站负载变化
- 其他UAV的切换改变了资源状态

**执行阶段的二次过滤是为了应对这种动态性**。

## 六、当前切换成功率的合理性问题

### 问题：分母不完整

```
切换成功率 = handover_successes / handover_attempts

其中：
- handover_attempts：只包含预测≥0.55的切换
- execution_filter_stats['below_threshold']：预测<0.55的切换（不计入分母）
```

**举例**：
```
决策阶段通过并传给执行阶段：100次
  ├─ 预测≥0.55（count_as_attempt=True）：90次 → handover_attempts
  │    ├─ 成功：90次 → handover_successes
  │    └─ 失败：0次
  └─ 预测<0.55（count_as_attempt=False）：10次 → execution_filter_stats

切换成功率 = 90 / 90 = 100%
```

但如果把所有传给执行阶段的都计入分母：
```
切换成功率 = 90 / 100 = 90%
```

### 你的观点：只要进入执行阶段就应该被计入

**你说得对！**

从逻辑上讲：
1. `execute_handover`被调用 = 系统决定尝试切换
2. 应该统计所有被调用的切换（不管预测高低）
3. 如果预测<0.55，可以**执行并统计失败**，而不是**不执行不统计**

### 当前实现的逻辑缺陷

```python
if success_prob < 0.55:
    # 不执行切换，也不计入统计
    return False
```

**问题**：
- 这不是"智能预测避免失败"，而是"选择性统计"
- 如果预测<0.55的切换实际成功率是50%，那真实成功率应该是：
  ```
  (90 + 10×0.5) / 100 = 95%
  ```
  而不是当前统计的100%

## 七、对比：传统算法的统计方式

```python
# line 90
def execute_handover(self, uav_id: int, target_bs_id: int, downgrade_ratio: float):
    self.handover_attempts += 1  # 所有调用都计入！

    # 直接尝试，没有预测过滤
    if target_bs.allocate(uav_id, required_rate):
        self.handover_successes += 1
        return True
    else:
        # 尝试抢占
        ...
```

**传统算法**：
- 所有`execute_handover`调用都计入`handover_attempts`
- 不预测，直接执行
- 成功率反映**所有尝试的真实比例**

**传统算法96.6%的含义**：
- 所有尝试的切换中，96.6%成功
- 3.4%失败是因为：
  1. 分配失败
  2. 抢占失败

## 八、合理的切换成功率定义

### 定义1：基于执行的切换（推荐）

```python
切换成功率 = 实际执行成功的切换 / 所有实际执行的切换
           = handover_successes / handover_attempts

其中：
- handover_attempts = 所有调用execute_handover的次数
- handover_successes = 执行成功的次数
```

**实现**：
```python
def execute_handover(self, uav_id: int, target_bs_id: int, downgrade_ratio: float):
    # 所有调用都计入
    self.handover_attempts += 1

    # 可以使用预测，但预测不影响统计
    success_prob = self.predict_handover_success(uav, target_bs_id, downgrade_ratio)

    # 如果预测很低，可以选择不执行（但仍已计入尝试）
    if success_prob < 0.55:
        self.execution_filter_stats['below_threshold'] += 1
        return False  # 记录为失败

    # 执行切换
    if target_bs.allocate(uav_id, required_rate):
        self.handover_successes += 1
        return True
    else:
        # ...
```

### 定义2：基于决策的切换

```python
切换成功率 = 决策并成功执行的切换 / 决策的切换
           = handover_successes / (handover_attempts + execution_filter_stats['below_threshold'])
```

**问题**：无法区分"决策阶段过滤"和"执行阶段过滤"

### 定义3：分离的指标

```python
# 指标1：预测准确性
预测准确率 = (预测≥0.55的次数) / 所有决策

# 指标2：执行成功率
执行成功率 = handover_successes / handover_attempts

# 指标3：综合成功率
综合成功率 = handover_successes / (handover_attempts + execution_filter_stats['below_threshold'])
```

## 九、当前实现的统计偏差

### 统计漏斗

```
决策调用：decision_calls = 100
  ↓
决策通过：90次（10次被过滤）
  ↓
进入执行：90次
  ├─ 预测≥0.55：85次 → handover_attempts
  └─ 预测<0.55：5次 → execution_filter_stats（不计入分母）
     ↓
执行成功：85次 → handover_successes

当前统计：
  切换成功率 = 85 / 85 = 100%

合理统计：
  切换成功率 = 85 / 90 = 94.4%
```

### 结论

**当前100%切换成功率的问题**：
1. 分母不完整（`execution_filter_stats['below_threshold']`不计入）
2. 这是**选择性统计**，而非真实性能
3. 预测<0.55的切换被"隐式过滤"，没有机会证明它们是否会失败

**关键区别**：
- 决策阶段的过滤（`missed_opportunity`）：主动放弃，是合理的智能决策
- 执行阶段的过滤（`execution_filter_stats['below_threshold']`）：隐式放弃，是统计偏差

## 十、修复方案

### 方案A：移除执行阶段的统计过滤（推荐）

```python
def execute_handover(self, uav_id: int, target_bs_id: int, downgrade_ratio: float) -> bool:
    # 所有调用都计入尝试
    self.handover_attempts += 1

    # 计算预测（用于记录，不影响统计）
    success_prob = self.predict_handover_success(uav, target_bs_id, downgrade_ratio)
    self.prediction_history.append(success_prob)

    # 可以根据预测决定是否执行（但不影响统计）
    if success_prob < 0.55:
        self.execution_filter_stats['below_threshold'] += 1
        # 仍然返回False（记录为失败），但已计入handover_attempts
        return False

    # 执行切换
    ...
```

### 方案B：移除执行阶段的预测过滤

```python
def execute_handover(self, uav_id: int, target_bs_id: int, downgrade_ratio: float) -> bool:
    # 所有调用都计入
    self.handover_attempts += 1

    # 移除预测过滤，直接执行
    target_bs = self.env.base_stations[target_bs_id]
    required_rate = uav.required_rate * downgrade_ratio

    # 直接尝试分配
    if target_bs.allocate(uav_id, required_rate):
        ...
```

### 方案C：分离预测和执行指标

```python
def get_detailed_stats(self) -> Dict:
    return {
        # 原有指标
        'handover_success_rate': self.handover_successes / max(self.handover_attempts, 1),

        # 新增指标
        'prediction_filter_rate': self.execution_filter_stats.get('below_threshold', 0) / max(self.handover_attempts + self.execution_filter_stats.get('below_threshold', 0), 1),
        'total_handover_requests': self.handover_attempts + self.execution_filter_stats.get('below_threshold', 0),
    }
```

## 十一、总结

### 关键发现

1. **决策阶段**：
   - 已经基于预测成功率筛选候选（`success_prob >= 0.55`）
   - 有兜底机制，可能传入预测<0.4的候选

2. **执行阶段**：
   - **再次计算预测**（可能不同）
   - 预测<0.55时：不计入`handover_attempts`，不执行切换
   - 这是**统计过滤**，不是决策优化

3. **切换成功率分母**：
   - 只包含预测≥0.55的切换
   - 不包含预测<0.55的切换
   - **导致虚高的100%**

### 你的核心观点

**"只要进入执行阶段的切换就应该被计入可能切换作为分母"**

这是完全正确的！

- `execute_handover`被调用 = 系统决定尝试切换
- 应该统计所有调用（不管预测高低）
- 可以根据预测决定是否执行，但**不应影响统计**

### 建议

**立即查看**：
1. `execution_filter_stats['below_threshold']`的数量
2. `handover_attempts`的数量
3. 两者的比例

如果`below_threshold`占比较大（如>10%），说明统计偏差明显。

然后选择修复方案A，恢复真实的切换成功率统计。
