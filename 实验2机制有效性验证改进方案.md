# 实验2机制有效性验证改进方案

## 修改日期
2026-03-24

## 修改背景

### 问题分析

#### 1. 负载均衡机制失效
实验2使用"禁用机制"的方法测试各机制贡献,结果显示:
- 禁用动态阈值: -1.5% ✅ 合理
- 禁用业务特化权重: -1.8% ✅ 合理
- 禁用ε-greedy探索: -4.8% ✅ 合理
- **禁用负载均衡: +0.2%** ❌ 不合理(应为负值)
- 禁用自适应识别更新: -2.1% ✅ 合理

**根本原因**:
1. 触发阈值过严 (`load_std < 0.15` 不执行)
2. 实验规模较小 (8基站, 50UAV), 负载波动小
3. 执行间隔过长 (每20步), 触发率低
4. 保守策略 (SINR损失容忍度低)

#### 2. 禁用方法的缺陷
- 机制间可能存在协同效应
- 单独禁用无法体现累积效果
- 不符合"从简单到复杂"的设计思路

### 决策

#### 不调整规模的原因
1. 扩大规模会显著增加运行时间
2. 当前规模已能验证大部分机制
3. 负载均衡的问题可以通过改进实验设计缓解
4. 先优化实验逻辑更符合当下利益

#### 改进方案
采用**从传统算法逐步添加机制**的方式:
1. 更符合算法设计的演进逻辑
2. 能清晰展示每个机制的独立贡献
3. 可观察机制间的累积效应
4. 避免禁用方法的协同效应问题

---

## 修改内容

### 1. 实验1注释补充

**文件**: `uav_system/experiments.py` (第45-52行)

**修改前**:
```python
print("\n实验目的：验证业务识别准确率对系统性能的影响")
print("\n实验条件：")
print("  A. 100%准确率 - 使用真实类型（性能基准）")
print("  B.  90%准确率 - 高质量识别模型")
print("  C.  80%准确率 - 中等质量识别模型")
print("  D.  60%准确率 - 低质量识别模型")
print("  E.  33%准确率 - 随机分配（下界对照）")
print("\n控制变量：差异化QoS配置、切换算法、网络环境完全相同")
```

**修改后**:
```python
print("\n实验目的：验证业务识别准确率对系统性能的影响")
print("\n实验条件：")
print("  A. 100%准确率 - 使用真实类型（性能基准）")
print("  B.  90%准确率 - 高质量识别模型")
print("  C.  80%准确率 - 中等质量识别模型")
print("  D.  60%准确率 - 低质量识别模型")
print("  E.  33%准确率 - 随机分配（下界对照）")
print("\n控制变量：差异化QoS配置、切换算法、网络环境完全相同")
print("\n算法配置说明：使用EnhancedHandover算法，但禁用ε-greedy探索机制")
print("  原因：ε-greedy引入随机性，会干扰识别准确率对性能影响的评估")
```

**说明**: 补充了禁用ε-greedy的原因,避免误解为设计缺陷。

---

### 2. 实验2机制定义修改

**文件**: `uav_system/experiments.py` (第870-886行)

**修改前**:
```python
MECHANISMS = {
    'full': '完整增强算法',
    'no_dynamic_threshold': '禁用动态阈值',
    'no_business_weights': '禁用业务特化权重',
    'no_epsilon_greedy': '禁用ε-greedy探索',
    'no_load_balance': '禁用负载均衡',
    'no_adaptive_recognition': '禁用自适应识别更新',
    'traditional': '传统算法（基线）'
}
```

**修改后**:
```python
MECHANISMS = {
    'traditional': '传统算法（基线）',
    'add_dynamic_threshold': '传统+动态阈值',
    'add_business_weights': '传统+动态阈值+业务权重',
    'add_epsilon_greedy': '传统+动态阈值+业务权重+ε-greedy',
    'add_load_balance': '传统+动态阈值+业务权重+ε-greedy+负载均衡',
    'add_adaptive_recognition': '传统+动态阈值+业务权重+ε-greedy+负载均衡+自适应识别',
    'full': '完整增强算法'
}
```

**说明**: 改为逐步添加机制的定义,更符合逻辑顺序。

---

### 3. 实验2运行逻辑重构

**文件**: `uav_system/experiments.py` (第888-937行)

**主要修改**:
1. 添加实验说明,展示机制添加顺序
2. 根据机制配置动态启用/禁用功能
3. 保持配置的逐步累积特性

**实现细节**:
```python
# 根据机制配置启用对应的功能
if mechanism == 'add_dynamic_threshold':
    # 只启用动态阈值，禁用其他增强功能
    algo.base_threshold = 0.005
    algo.calculate_dynamic_threshold = algo.__class__.calculate_dynamic_threshold.__get__(algo, type(algo))
    # 禁用业务权重
    for bt in BusinessType:
        algo.business_weights[bt] = {'sinr': 0.4, 'load': 0.3, 'rate': 0.3}
    # 禁用ε-greedy
    algo.epsilon = 0.0
    # 禁用负载均衡（在运行时控制）
    enable_lb = False

elif mechanism == 'add_business_weights':
    # 启用动态阈值和业务权重，禁用其他
    algo.epsilon = 0.0
    enable_lb = False

elif mechanism == 'add_epsilon_greedy':
    # 启用动态阈值、业务权重和ε-greedy
    # epsilon默认为0.05，无需修改
    enable_lb = False

elif mechanism == 'add_load_balance':
    # 启用动态阈值、业务权重、ε-greedy和负载均衡
    enable_lb = True

elif mechanism == 'add_adaptive_recognition':
    # 启用所有机制，包括自适应识别更新
    # 自适应识别由环境控制，无需额外配置
    enable_lb = True

elif mechanism == 'full':
    # 完整增强算法
    enable_lb = True
```

---

### 4. 实验2结果分析改进

**文件**: `uav_system/experiments.py` (第953-1000行)

**修改内容**:
1. 改为"逐步添加机制贡献分析"
2. 显示每个阶段的性能变化
3. 计算总体提升

**输出示例**:
```
【逐步添加机制贡献分析】

相对于传统算法的逐步提升:
  动态阈值: 贡献 = +0.0150 (+1.8%) [0.8500 -> 0.8650]
  业务权重: 贡献 = +0.0100 (+1.2%) [0.8650 -> 0.8750]
  ε-greedy探索: 贡献 = +0.0400 (+4.6%) [0.8750 -> 0.9150]
  负载均衡: 贡献 = +0.0050 (+0.5%) [0.9150 -> 0.9200]
  自适应识别更新: 贡献 = +0.0200 (+2.2%) [0.9200 -> 0.9400]
  完整算法(验证): 贡献 = +0.0000 (+0.0%) [0.9400 -> 0.9400]

总体提升: 传统算法 -> 完整增强算法 = +10.6%
```

---

### 5. 实验2可视化重构

**文件**: `uav_system/experiments.py` (第1002-1078行)

**图表改进**:

1. **逐步提升曲线图** (axes[1,0]):
   - X轴: 机制添加阶段
   - Y轴: 整体满足率
   - 显示性能逐步提升的过程
   - 标注每个阶段的提升幅度

2. **独立贡献柱状图** (axes[1,1]):
   - X轴: 各机制
   - Y轴: 满足率提升
   - 清晰展示每个机制的独立贡献
   - 正值=正向贡献, 负值=负向影响

3. **综合评分对比** (axes[1,2]):
   - 展示各配置的综合性能评分
   - 权重: 满足率40% + 成功率30% + 关键业务30%

---

## 预期效果

### 1. 更清晰的机制贡献分析
- 每个机制的性能提升一目了然
- 可以识别哪些机制贡献最大/最小
- 便于优化算法设计

### 2. 更符合逻辑的实验设计
- 从简单到复杂的演进过程
- 符合实际算法开发的流程
- 结果更具说服力

### 3. 解决负载均衡失效问题
- 虽然不扩大规模,但通过"逐步添加"的方式
- 可以观察负载均衡的独立效果
- 如果仍然显示负贡献,则明确说明该机制在小规模下无效

### 4. 可扩展性
- 机制添加顺序可以灵活调整
- 可以轻松添加新机制测试
- 为未来扩规模预留接口

---

## 后续建议

### 1. 负载均衡优化
如果实验结果显示负载均衡贡献很小或为负,考虑:
- 降低触发阈值 (`load_std < 0.10`)
- 缩短执行间隔 (每10步)
- 在大规模场景下单独测试

### 2. 机制交互分析
未来可增加:
- 机制组合的协同效应测试
- 不同机制添加顺序的对比
- 机制间是否存在冗余或冲突

### 3. 实验规模扩展
如需更准确地评估负载均衡:
- 扩大到15基站、100UAV
- 测试不同规模下的机制有效性
- 分析机制对规模的敏感性

---

## 修改总结

| 修改项 | 修改位置 | 修改类型 | 影响 |
|--------|---------|---------|-----|
| 实验1注释补充 | 第45-52行 | 添加说明 | 避免误解 |
| 机制定义重构 | 第878-886行 | 逻辑修改 | 核心改进 |
| 运行逻辑重构 | 第888-937行 | 算法修改 | 核心改进 |
| 结果分析改进 | 第953-1000行 | 输出修改 | 用户体验 |
| 可视化重构 | 第1002-1078行 | 图表修改 | 结果展示 |

**核心改进**: 从"禁用机制"改为"逐步添加机制",更符合逻辑且能准确评估各机制贡献。
