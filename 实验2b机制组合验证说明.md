# 实验2b：机制组合验证

## 实验目的

解决实验2发现的矛盾现象:
- ε-greedy在"禁用"时是正面的(+4.8%)
- ε-greedy在"逐步添加"时是负面的(-4.7%)
- 负载均衡的作用取决于组合方式

通过测试不同机制组合,验证机制间的交互效应,找到最优配置。

---

## 实验设计

### 测试组合 (15种)

| 分类 | 组合 | 说明 |
|------|------|------|
| **基线** | traditional | 传统算法 |
| **单机制** | dyn_thresh | +动态阈值 |
| | weights | +业务权重 |
| | epsilon | +ε-greedy |
| | load_balance | +负载均衡 |
| **双机制** | dyn_thresh_weights | 动态阈值+业务权重 |
| | dyn_thresh_epsilon | 动态阈值+ε-greedy |
| | weights_epsilon | 业务权重+ε-greedy |
| **三机制** | dyn_thresh_weights_epsilon | 动态阈值+业务权重+ε-greedy |
| | dyn_thresh_weights_lb | 动态阈值+业务权重+负载均衡 |
| | dyn_thresh_epsilon_lb | 动态阈值+ε-greedy+负载均衡 |
| | weights_epsilon_lb | 业务权重+ε-greedy+负载均衡 |
| **四机制** | dyn_thresh_weights_epsilon_lb | 动态阈值+业务权重+ε-greedy+负载均衡 |
| **完整** | full | 完整增强算法(含自适应识别) |

### 实验参数

- 网络规模: 8基站, 50UAV (与实验2相同)
- 仿真步数: 150步
- 重复次数: 8次 (减少以加快运行)
- 随机事件概率: 5%

---

## 关键分析

### 1. 机制贡献分析

计算每个单机制相对于传统算法的性能提升:
```
动态阈值: 满足率提升 = sat(dyn_thresh) - sat(traditional)
业务权重: 满足率提升 = sat(weights) - sat(traditional)
ε-greedy: 满足率提升 = sat(epsilon) - sat(traditional)
负载均衡: 满足率提升 = sat(load_balance) - sat(traditional)
```

### 2. ε-greedy作用对比

对比相同基线下,含/不含ε-greedy的性能:
- dyn_thresh vs dyn_thresh_epsilon
- weights vs weights_epsilon
- dyn_thresh_weights vs dyn_thresh_weights_epsilon
- dyn_thresh_weights_lb vs dyn_thresh_weights_epsilon_lb

**计算指标**:
```
含ε-greedy的平均满足率 = mean([dyn_thresh_epsilon, weights_epsilon, ...])
不含ε-greedy的平均满足率 = mean([dyn_thresh, weights, ...])
```

### 3. 负载均衡作用对比

对比相同基线下,含/不含负载均衡的性能:
```
无负载均衡: [dyn_thresh, weights, dyn_thresh_weights, dyn_thresh_epsilon, ...]
有负载均衡: [dyn_thresh_weights_lb, dyn_thresh_epsilon_lb, ...]
```

### 4. 最优组合识别

通过性能排序找出最优配置,并分析:
- 最优组合包含哪些机制?
- 是否比完整算法更优?
- 相对传统算法提升多少?

---

## 输出内容

### 1. 表格输出

#### 按性能排序
```
排名 | 组合名称 | 整体满足率 | 切换成功率 | 关键业务满足率 | 提升(相对传统)
-----|---------|-----------|-----------|--------------|------------
1    | 动态阈值+业务权重 | 0.8700 | 95.20% | 0.8500 | +32.00%
2    | 动态阈值 | 0.8600 | 94.80% | 0.8400 | +30.40%
...
```

#### 单机制贡献
```
动态阈值: +0.2001 (+30.3%)
业务权重: +0.0093 (+1.1%)
ε-greedy: -0.0411 (-4.7%)
负载均衡: +0.0016 (+0.2%)
```

### 2. 关键发现

```
1. 最优组合: 动态阈值+业务权重 (满足率=0.8700)
2. 相对传统算法提升: 32.00%
3. 相对完整算法差异: -0.0093
   [验证] 完整算法接近最优组合

【ε-greedy机制分析】
  含ε-greedy的平均满足率: 0.8300
  不含ε-greedy的平均满足率: 0.8500
  差异: -0.0200
  结论: ε-greedy在当前规模下总体起负面作用
```

### 3. 可视化输出

生成 `exp2b_results.png`,包含7个子图:

#### 图1: 机制组合性能排名 (横条图)
- Y轴: 所有15种组合
- X轴: 整体满足率
- 颜色: 从红到绿(RdYlGn)

#### 图2: 单机制性能热力图
- X轴: 5种单机制
- 颜色: 性能值
- 标注: 具体数值

#### 图3: 双机制组合性能矩阵
- X/Y轴: 4种核心机制
- 内容: 组合的性能值
- 颜色: 性能高低

#### 图4: ε-greedy作用对比 (分组柱状图)
- 4组对比: 同一基线下含/不含ε-greedy
- 蓝色: 不含ε-greedy
- 灰色: 含ε-greedy

#### 图5: 负载均衡作用对比 (柱状图)
- 两根柱子: 无负载均衡 vs 有负载均衡
- 标注: 平均提升百分比

#### 图6: 切换成功率 vs 整体满足率 (散点图)
- X轴: 切换成功率
- Y轴: 整体满足率
- 颜色: 性能高低
- 标记: 最优组合(金色星形)

#### 图7: 关键文本摘要
- 最优组合及性能
- 相对传统/完整算法的差异
- 核心机制列表
- ε-greedy结论
- 负载均衡结论

---

## 预期结果分析

### 场景1: 动态阈值+业务权重 = 最优

**证据**:
- 实验2: 动态阈值(+30.3%) + 业务权重(+1.1%) = 0.8688
- 实验2b: 该组合应该在排名前3

**结论**:
- 这两个机制协同效果最好
- 不需要ε-greedy和负载均衡
- 自适应识别更新贡献微小

### 场景2: ε-greedy总体负面

**证据**:
- 含ε-greedy的平均 < 不含ε-greedy的平均
- 大部分含ε-greedy的组合性能更低

**原因**:
- 小规模网络(8基站)下,局部最优已接近全局最优
- 随机探索破坏稳定性,引入额外切换
- ε=0.05的探索率偏高

**建议**:
- 当前规模下禁用ε-greedy
- 扩大规模后重新评估(12+基站)

### 场景3: 负载均衡作用有限

**证据**:
- 单独添加负载均衡: +0.2%
- 平均提升: 0-2%

**原因**:
- 触发条件过严 (`load_std < 0.15`)
- 执行间隔过长 (每20步)
- 小规模负载波动小,很少触发

**建议**:
- 当前规模下可选
- 扩大规模后效果会更明显

### 场景4: 完整算法 = 接近最优

**证据**:
- 完整算法排名: 前2-3名
- 与最优组合差异: <1%

**结论**:
- 完整算法是"稳健"的选择
- 性能损失很小(1-2%)
- 避免了手动调参的风险

---

## 后续建议

### 1. 如果发现 `动态阈值+业务权重` 明显优于完整算法

**行动**:
1. 分析ε-greedy和负载均衡的具体负面影响
2. 考虑在完整算法中动态启用/禁用这些机制
3. 根据网络规模自适应调整配置

**论文呈现**:
- "在中小规模网络中,动态阈值+业务权重组合达到最优"
- "完整算法虽然性能略低,但具有更好的泛化性"

### 2. 如果ε-greedy在部分组合中有效

**行动**:
1. 分析有效的组合特征
2. 找出ε-greedy的正向触发条件
3. 设计自适应的探索策略

**论文呈现**:
- "ε-greedy的作用取决于网络优化程度"
- "在未优化网络中,探索有助于跳出局部最优"

### 3. 如果负载均衡无显著作用

**行动**:
1. 降低触发阈值到0.10
2. 缩短执行间隔到每10步
3. 在大规模场景(15基站, 120UAV)下重新测试

**论文呈现**:
- "负载均衡在大规模网络中作用更明显"
- "当前规模下贡献有限,但为扩展性提供了保障"

---

## 运行方式

### 运行实验2b

```python
# main.py
main(force_retrain=False, run_experiments=['2b'])
```

### 输出文件

- `exp2b_results.png`: 完整可视化结果
- 控制台: 性能排序、贡献分析、关键发现

---

## 技术细节

### 机制配置逻辑

```python
# 根据组合名称解析包含的机制
has_dyn_thresh = 'dyn_thresh' in combo_name
has_weights = 'weights' in combo_name
has_epsilon = 'epsilon' in combo_name
has_lb = 'load_balance' in combo_name or 'lb' in combo_name

# 禁用/启用相应机制
if not has_dyn_thresh:
    # 固定阈值
    algo.calculate_dynamic_threshold = lambda uav: 0.005

if not has_weights:
    # 通用权重
    algo.business_weights[bt] = {'sinr': 0.4, 'load': 0.3, 'rate': 0.3}

if not has_epsilon:
    # 禁用探索
    algo.epsilon = 0.0

enable_lb = has_lb
```

### 性能评估指标

- **整体满足率** (40%权重)
- **切换成功率** (30%权重)
- **关键业务满足率** (30%权重)

综合评分 = 0.4 × 满足率 + 0.3 × 成功率 + 0.3 × 关键业务

---

## 总结

实验2b通过系统地测试15种机制组合,能够:

1. ✅ 验证机制间的交互效应
2. ✅ 找到当前规模下的最优配置
3. ✅ 解释实验2中发现的矛盾
4. ✅ 为算法优化提供明确方向
5. ✅ 为大规模扩展提供理论依据

**预期耗时**: ~15-20分钟 (15种组合 × 8次重复 × 150步)
