# 第3章 切换算法设计与实现

## 3.1 切换算法设计总体思路

在上一章所建立的仿真环境、业务识别模块和满意度评估模型的基础上，本章聚焦于无人机切换决策算法的设计与实现。切换算法是整个系统的核心决策引擎，其性能直接决定了无人机在基站间的通信连续性和服务质量保障水平。本章按照从传统到增强、从启发式到学习式的递进逻辑，依次设计并实现了三种切换算法：基于3GPP标准的传统切换算法作为性能基线，引入业务感知与多机制协同的增强切换算法作为核心贡献方案，以及基于深度强化学习的DQN切换决策方法作为探索性扩展。三种算法的设计既保持了接口上的一致性，又在决策机制上各具特色，构成了一个由简到繁、逐层递进的算法体系。

值得指出的是，三种算法并非完全并列的竞争关系，而更应被视为不同抽象层次上的解决方案。传统算法代表了5G标准协议中的工程实践方案，增强算法在传统方案的基础上引入业务感知能力，而DQN方法则探索了数据驱动的端到端决策范式。这种层次化的设计使得系统能够在统一框架下对不同类型的切换策略进行公平比较，也为后续的消融实验和机制有效性分析提供了丰富的对比维度。

## 3.2 传统切换算法设计

### 3.2.1 3GPP A3事件触发机制

传统切换算法的设计目标是忠实复现3GPP协议中定义的标准化切换流程，作为后续增强算法的性能对比基线。在3GPP LTE/5G网络架构中，切换决策由A3事件（即邻区优于服务小区事件）触发，其核心判定条件为：当邻区的信号质量超过服务小区的信号质量一定裕量时，触发切换准备流程[1]。具体而言，A3事件的触发条件可表示为：

$$\text{SINR}_{neighbor} > \text{SINR}_{serving} + H_{ys} + O_{fn}$$

其中，$\text{SINR}_{neighbor}$为邻区的信号与干扰加噪声比，$\text{SINR}_{serving}$为服务小区的SINR值，$H_{ys}$为迟滞参数（Hysteresis），$O_{fn}$为频率偏移参数（Offset）。迟滞参数的引入是为了抑制乒乓效应（ping-pong effect），即防止无人机在两个基站之间因信号质量的微小波动而频繁切换。在本系统中，迟滞参数$H_{ys}$设为2dB，频率偏移$O_{fn}$设为0dB，这些取值参考了3GPP TS 36.331中的推荐配置范围[2]。

传统算法的决策流程可以概括为两个阶段。在切换触发阶段，算法遍历所有邻区基站，选择SINR最高的候选基站，判断其是否满足A3事件的触发条件。若满足，则将该基站作为切换目标；若不满足，则维持当前连接不变。在切换执行阶段，算法采用"先断后连"（break-before-make）策略，即先释放无人机在服务小区占用的资源，再尝试在目标基站完成资源分配。这种策略实现简单，但存在一个固有缺陷：一旦释放旧基站的资源后目标基站分配失败，无人机将直接进入断连状态，没有任何回退机制。

### 3.2.2 传统算法的局限性分析

传统A3事件触发算法虽然在实际网络中得到了广泛部署，但在无人机通信场景下面临几个结构性局限。首先，该算法仅以SINR作为切换决策的唯一依据，完全不考虑目标基站的负载状况。当目标基站负载较高甚至过载时，即便其信号质量满足A3条件，切换后的服务质量仍可能因资源竞争而急剧下降。其次，传统算法对所有无人机采用完全相同的切换策略，不区分业务类型的差异化需求。对于控制信令类无人机，即使在服务小区的SINR尚可接受但略有下降的情况下，算法也可能因为A3条件触发而执行不必要的切换，增加了通信中断的风险。而对于视频回传类无人机，算法可能因为迟滞参数的存在而错过了更优的目标基站，导致带宽资源分配不足。

此外，传统算法的切换执行采用单次分配尝试机制，不进行速率降级或资源抢占。当目标基站的可用容量不足以满足无人机的完整速率需求时，切换直接失败，无人机被迫断连。在多业务共存的场景下，这种非此即彼的分配策略显得过于僵化——一个低优先级的环境监测无人机仅因目标基站暂时的容量不足而被迫断连，而此时基站上可能正连接着大量高优先级无人机，存在通过降级或重分配来优化整体资源利用的空间。上述局限性为增强切换算法的设计提供了明确的改进方向。

## 3.3 增强切换算法设计

针对传统算法在业务感知、资源利用和切换鲁棒性方面的不足，本节设计了一套增强切换算法。增强算法并非对传统算法的简单修补，而是从决策机制和执行保障两个层面进行了系统性的重构。在决策层面，引入业务感知效用函数替代纯粹的SINR比较，通过动态切换阈值机制实现差异化的切换触发策略。在执行层面，设计了降级比例搜索、优先级抢占、回滚保护和软迁移等多级保障机制，形成从资源请求到失败恢复的完整处理链路。此外，周期性执行的全局负载均衡机制进一步从网络整体层面优化资源分配的公平性。

### 3.3.1 业务感知效用函数

效用函数是增强算法决策过程的核心组件，负责将多维度网络状态信息综合为一个标量化的效用值，以支持不同候选基站之间的量化比较。与传统算法仅依赖SINR不同，效用函数同时考量信号质量、负载状况和速率匹配度三个维度，并根据无人机的业务类型自适应调整各维度的权重。

效用值的计算公式为：

$$U = w_{sinr} \cdot \hat{S} + w_{load} \cdot (1 - \rho) + w_{rate} \cdot M + B$$

其中，$\hat{S}$为归一化SINR值（将原始SINR线性映射至$[0,1]$区间），$\rho$为目标基站的负载率（$0 \leq \rho \leq 1$），$M$为速率匹配度（基于可用容量与需求速率的比值，通过指数函数$1 - e^{-3\min(\alpha, 1.5)}$计算，其中$\alpha$为可用容量与需求速率的比值），$B$为高速率奖励项（当降级比例不低于80%时给予额外激励）。$w_{sinr}$、$w_{load}$和$w_{rate}$分别为三个维度的权重系数，其取值根据无人机的业务类型确定。

三类业务的权重配置反映了各自的核心诉求。控制信令类业务赋予信号质量最高的权重（$w_{sinr}=0.5$），因为稳定的信号连接是保障低时延控制指令传输的基础，同时赋予负载状况较低的权重（$w_{load}=0.2$），反映了该类业务低带宽需求的特点。视频回传类业务赋予速率匹配度最高的权重（$w_{rate}=0.45$），因为充足的带宽资源是保障视频流质量的关键，信号质量权重降至0.3。环境监测类业务的速率权重进一步提升至0.5，信号质量权重降至0.25，体现了该类业务对带宽稳定性的重视程度超过对信号瞬时质量的要求。

这种业务特化的权重设计使得效用函数能够针对不同业务类型的实际需求进行差异化评估。例如，当两个候选基站的SINR值相近但负载率差异较大时，控制信令类无人机会倾向于选择SINR更优的基站（即使其负载率略高），而视频回传类无人机则可能倾向于选择负载更低从而能分配更多带宽的基站。这种差异化的评估视角是传统纯SINR驱动算法所不具备的。

### 3.3.2 动态切换阈值机制

在传统算法中，A3事件的迟滞参数是固定值（2dB），所有无人机共用同一个切换触发门槛。然而，不同业务类型对切换时机的需求存在显著差异：高优先级业务应在信道质量初期恶化时即启动切换，以预留充足的处理时间；低优先级业务则可以容忍一定程度的信号下降，避免过于频繁的切换。基于此，增强算法引入了动态切换阈值机制，替代传统算法中固定的迟滞参数。

动态阈值的计算综合考虑了业务优先级、当前基站负载率、识别置信度和无人机移动性四个因素。其计算逻辑可以描述如下。首先，以一个基础阈值（设为-0.002）为起点。对于控制信令类业务，基础阈值折半，使得该类业务更容易触发切换——这一设计反映了控制信令业务"宁可早切换、不可晚切换"的策略取向。其次，引入负载因子：当无人机当前所连接的基站负载率升高时，切换阈值相应降低，即更容易触发切换，以促使无人机提前离开高负载基站。对于控制信令业务，当负载率超过70%时，阈值降低幅度加大，体现了对关键业务的额外保护。第三，识别置信度因子对阈值进行微调：置信度越低，阈值越低（切换越容易触发），这是因为在识别结果不确定时，早切换比晚切换的风险更低。第四，移动性因子根据无人机的飞行速度调整阈值：速度越快，阈值越低，以适应高速移动场景下切换窗口较窄的特点。控制信令类业务还会叠加一个额外的优先级降低因子，进一步降低其切换门槛。

最终，动态阈值被限定在一个下限之上（控制信令为0.0025，其他业务为0.005），以防止阈值过低导致无意义的频繁切换。在决策过程中，只有当最优候选基站的效用值超过当前基站效用值与动态阈值之和时，才触发切换。这种设计使得切换触发条件能够根据上下文动态调整，而非依赖一个全局固定的门槛。

### 3.3.3 降级比例搜索与资源分配策略

当效用函数确定了最优候选基站后，增强算法并不像传统算法那样仅以完整速率请求资源分配，而是执行降级比例搜索。该机制的核心理念是：在目标基站可用容量不足以满足完整速率需求时，以降低的速率接入仍优于完全断连。这一理念源于对不同业务类型的差异化降级容忍度的认知——视频回传业务在速率降至理想速率的60%时仍可维持基本可用性，环境监测业务甚至可以接受降至30%的速率，而控制信令业务仅能容忍5%的降级。

具体而言，系统为每类业务定义了一组可行的降级比例。控制信令类业务的降级比例为$[1.0, 0.95, 0.9]$，仅允许极小幅度的降级；视频回传类业务为$[1.0, 0.9, 0.8, 0.7, 0.6]$，允许中等幅度的降级；环境监测类业务为$[1.0, 0.8, 0.6, 0.4, 0.3]$，允许较大幅度的降级。在决策过程中，算法对所有候选基站的所有可行降级比例组合计算效用值，选择效用值最高的（基站，降级比例）组合作为决策输出。

以一个8基站、5档降级比例的系统为例，每架无人机每个仿真步需要评估最多$7 \times 5 = 35$个（基站，降级比例）组合（排除当前已连接基站），从中选出效用值最优者。这种穷举搜索虽然增加了计算复杂度，但确保了在所有可能的资源分配方案中做出全局最优的选择。

### 3.3.4 多级切换执行保障机制

在切换执行阶段，增强算法设计了一套多级保障机制，通过层层递进的回退策略最大限度地保障切换成功率。整个执行流程按照"直接分配—抢占—回滚"的顺序逐级推进，每一级失败后自动进入下一级处理。

第一级为直接分配。算法首先释放无人机在旧基站占用的资源，然后以决策阶段确定的速率向目标基站请求资源分配。若目标基站的可用容量充足，分配直接成功，切换完成。这一级对应的是资源充裕条件下的理想路径。

第二级为优先级抢占。当直接分配因目标基站容量不足而失败时，算法检查目标基站上已连接的无人机列表，按优先级从低到高识别可以被抢占的候选。抢占过程遵循两个关键约束：关键程度不低于0.9的无人机（在本系统中仅为控制信令类）永远不会被抢占，且只有优先级低于当前切换无人机的无人机才可能被选中。被选中的无人机按优先级升序依次释放，直到释放的总空间满足当前无人机的速率需求，或遍历完所有候选。若释放的空间充足，当前无人机成功接入目标基站，而被抢占的无人机则进入断连状态，等待后续的软迁移处理。

第三级为回滚保护。当抢占也未能释放足够空间时，算法尝试回滚到旧基站。回滚过程首先检查旧基站在释放当前无人机资源后的可用容量是否足以恢复原有的速率分配，若足够则直接回滚。若旧基站的可用容量也不足（可能是因为在释放和重新分配的时间窗口内有其他无人机占用了空间），则尝试在旧基站上执行抢占操作来腾出空间。回滚机制的存在有效避免了传统算法中"先断后连"失败即断连的僵化处理方式，为切换操作提供了一道安全网。

第四级为软迁移。当抢占成功导致某些无人机被踢出时，算法并非简单地将这些无人机置为断连状态，而是为每架被抢占的无人机尝试寻找其他基站的接入机会。软迁移过程遍历所有基站（排除抢占发起的基站），对每座基站尝试各档降级比例的分配，选择综合评分最高的（SINR与可用容量的加权和）作为迁移目标。若找到可行目标，则立即完成迁移，被抢占的无人机无需经历断连状态。这一机制将抢占操作的副作用降至最低，体现了"抢占是手段而非目的"的设计哲学。

### 3.3.5 全局负载均衡机制

除上述以单架无人机为中心的切换决策和执行机制外，增强算法还引入了周期性执行的全局负载均衡机制，从网络整体层面优化资源分配的空间公平性。负载均衡机制每5个仿真步执行一次，其核心思想是将高负载基站上的低优先级无人机迁移到负载较轻的基站，从而缩小基站间负载率的差异。

负载均衡机制的执行流程如下。首先，计算所有基站负载率的标准差，若标准差低于0.05，则认为当前网络负载已足够均衡，跳过本轮均衡操作。否则，将所有基站按负载率降序排列，选取负载率最高的基站（源基站）和负载率最低的基站（目标基站）。若两者负载率之差低于10%，同样跳过本轮操作。接下来，在源基站的已连接无人机中筛选迁移候选，筛选条件包括：切换到目标基站后的SINR损失不超过5dB（确保信号质量不会因迁移而严重退化）、目标基站的可用容量不低于需求速率的30%、以及迁移后的效用值下降不超过0.05（确保迁移不会显著降低被迁移无人机的服务质量）。通过筛选的候选无人机按迁移优先级评分排序，评分综合考虑无人机的当前分配速率、业务优先级和关键程度——低优先级、低关键程度且占用较多资源的无人机优先被选中。最终，从排序后的候选列表中选取前5架无人机执行迁移操作。

负载均衡机制与单无人机切换决策的协同工作方式值得关注。单无人机的切换决策是一种分布式的、以个体效用最大化为目标的决策过程，可能存在多个无人机同时涌向同一低负载基站的情况，导致新的负载不均。周期性的全局负载均衡机制则从全局视角进行宏观调控，弥补了分布式决策在全局优化能力上的不足。两者的结合在微观和宏观两个层面共同维护着网络资源分配的合理性和公平性。

## 3.4 基于深度强化学习的切换决策方法

### 3.4.1 强化学习问题建模

传统切换算法和增强切换算法均属于启发式方法，其决策逻辑由人工设计的规则和阈值所驱动。虽然启发式方法在可解释性和实现复杂度方面具有优势，但其规则设计依赖于对场景特征的先验认知，难以自动适应复杂多变的网络环境。深度强化学习（Deep Reinforcement Learning, DRL）为切换决策提供了一种数据驱动的替代范式——智能体通过与环境的反复交互，自主学习在不同网络状态下应采取的最优切换策略。本节将无人机切换决策建模为马尔可夫决策过程（Markov Decision Process, MDP），并基于DQN算法实现端到端的切换策略学习。

在强化学习建模中，状态空间（State Space）的设计至关重要，它决定了智能体能够感知的环境信息范围。本系统的状态空间采用多维拼接的方式构建，以目标无人机为中心，涵盖局部信道状态、基站负载信息、自身业务特征和全局资源紧张度等多个层面的信息。具体而言，状态向量包含以下分量：各基站的SINR值（归一化至$[0,1]$区间，共$num_{bs}$维），各基站的当前负载率（$num_{bs}$维），当前连接基站的one-hot编码（$num_{bs}$维），各基站可用容量与无人机需求速率的比值（$num_{bs}$维），业务类型的one-hot编码（3维），移动速度（归一化至$[0,1]$，1维），当前满意度（1维），连接状态（1维），切换冷却计时（归一化，1维），上次切换来源基站的one-hot编码（$num_{bs}$维），以及全局资源紧张度（所有基站平均负载率，1维）。以8个基站为例，完整状态向量的维度为$8 \times 5 + 8 = 48$维。

在上述状态分量中，容量比值特征、上次切换来源基站特征和全局资源紧张度特征是为本系统专门设计的新型状态信息，其有效性通过消融实验进行验证。容量比值特征让智能体能够直接感知每个基站是否能满足其资源需求，避免在SINR较好但容量不足的基站上浪费切换机会。上次切换来源基站特征帮助智能体记忆切换历史，有助于抑制乒乓效应。全局资源紧张度则提供了对网络整体资源状况的宏观认知，使智能体在资源充裕和紧张两种截然不同的环境中能够采取差异化的策略。

动作空间（Action Space）被设计为离散动作集合。动作0表示"保持当前连接不变"，动作1至$num_{bs}$分别表示"切换到编号为1至$num_{bs}$的基站"。以8个基站为例，动作空间共包含9个离散动作。动作空间的设计遵循了简化原则——在初始设计中曾为每个基站设置3档降级比例（全速率、80%、60%），动作空间维度达到$1+8 \times 3=25$，但实验发现过多的动作选项导致"保持"动作被探索的概率过低（仅4%），智能体倾向于频繁尝试切换。简化后的动作空间将"保持"动作的探索概率提升至11%，显著改善了训练稳定性。此外，系统还引入了动作掩码（Action Masking）机制，在每个决策时刻自动屏蔽"切换到当前已连接基站"的无效动作，避免智能体浪费探索预算在无意义的动作上。

### 3.4.2 奖励函数设计

奖励函数是强化学习系统中最关键的设计要素之一，它定义了智能体行为的优化目标，直接决定了学到的策略的质量。本系统的奖励函数采用多组分加权的结构，综合考量满意度变化、切换成本、连接稳定性等多个目标，并引入场景自适应机制来平衡不同资源紧张度下的奖励导向。

奖励函数的主干由满意度变化项构成，其权重为3.0。满意度变化即当前步满意度与上一步满意度之差，正值表示服务质量改善，负值表示退化。将满意度变化作为主奖励，引导智能体学习"选择能最大化满意度增量的切换策略"这一核心行为模式。此外，当满意度绝对值高于0.3的基线时，额外给予满意度值的50%作为正向激励，鼓励智能体不仅追求满意度的提升，还要维持高满意度状态。

切换惩罚项用于抑制过于频繁的切换行为，防止乒乓效应。每次实际执行的切换施加0.8的基础惩罚。值得注意的是，切换惩罚并非固定值，而是通过场景自适应机制根据当前网络负载状况进行调节。当全网平均负载率低于50%时，惩罚系数为1.0，属于正常的切换成本；当负载率在50%至80%之间时，惩罚系数线性增至1.5，反映资源逐渐紧张时切换的风险增加；当负载率超过80%时，惩罚系数进一步增至最高2.5，强烈抑制在资源严重紧张时的切换冲动。这种自适应设计确保了智能体在资源充裕时敢于探索切换机会，在资源紧张时则保持保守策略。

除上述两项主要奖励组分外，奖励函数还包含以下辅助组分：断连惩罚（-3.0），在无人机从连接状态变为断连状态时施加，是所有惩罚项中最为严厉的，以强化智能体对连接保持的重视；维持连接奖励（+0.05），在每个保持连接的步给予小幅正向激励；降级惩罚，当分配速率低于理想速率时按缺口比例扣分；冷却惩罚，在距上次切换不足10步的冷却期内再次切换时额外扣分，冷却期越近惩罚越大；乒乓惩罚，在冷却期内切回上一个基站时额外施加0.5的惩罚。

综合来看，奖励函数的设计遵循了一个基本原则：让"保持连接"成为默认最优策略，仅在满意度显著下降（即存在更好的基站选择）时才值得承受切换成本。一次切换的总成本最高可达$0.8 \times 2.5 + 0.3 + 0.5 = 2.8$（高负载场景下的切换惩罚+冷却惩罚+乒乓惩罚），而"保持连接"每步的收益约为0.65。这意味着智能体需要预期满意度至少提升约$0.93$（$2.8/3.0$）才值得在高负载场景下执行切换，这一阈值设置确保了切换行为的审慎性。

### 3.4.3 DQN网络结构与训练策略

本系统采用DQN（Deep Q-Network）作为强化学习的核心算法框架。DQN由Mnih等人在2015年提出[3]，通过将深度神经网络与Q-learning相结合，实现了在高维状态空间下的端到端策略学习。相较于原始的Q-learning，DQN引入了经验回放（Experience Replay）和目标网络（Target Network）两项关键技术，有效解决了深度强化学习中的训练不稳定和样本效率低的问题。

Q网络的结构采用经典的三层全连接前馈架构：输入层接受状态向量（维度为48维），经过第一个隐藏层（128个神经元，ReLU激活函数），再经过第二个隐藏层（128个神经元，ReLU激活函数），最后由输出层产生每个动作的Q值估计（9维输出，对应9个离散动作）。这一网络结构在参数量和表达能力之间取得了平衡：两个128维的隐藏层足以拟合切换决策中复杂的非线性映射关系，同时又不至于因参数过多而导致过拟合。

在训练策略方面，系统采用了以下关键配置。经验回放缓冲区的容量设为50000条经验，每条经验包含状态、动作、奖励、下一状态、终止标志以及下一状态的无效动作掩码。训练时从缓冲区中随机采样批次大小为64的小批量数据进行梯度更新。采用SoftL1Loss（Huber Loss）作为损失函数，相较于均方误差（MSE），Huber Loss对Q值估计误差较大的样本施加线性惩罚而非二次惩罚，能够有效防止Q值发散时损失值的爆炸。梯度裁剪的范数上界设为1.0，进一步增强了训练的稳定性。学习率设为$5 \times 10^{-4}$，折扣因子$\gamma$设为0.95。折扣因子的取值略低于常见的0.99，这一选择有意使智能体更加关注短期奖励而非过于遥远的未来回报，在切换决策这一时序敏感的任务中有助于加速策略收敛。

目标网络的更新采用Polyak平均（Soft Update）策略，更新系数$\tau$设为0.005。每执行一次训练步骤，目标网络的参数按$\theta_{target} \leftarrow \tau \cdot \theta + (1-\tau) \cdot \theta_{target}$进行更新。相较于周期性硬拷贝的更新方式，Polyak平均使得目标网络的参数变化更为平滑，避免了TD目标值的突变导致的策略震荡。

探索策略采用$\varepsilon$-greedy方法，初始探索率$\varepsilon$从1.0开始，按$\varepsilon \leftarrow 0.995 \times \varepsilon$的指数衰减方式逐步降低至最终值0.01。在训练初期（$\varepsilon$较高时），智能体以较大概率随机选择有效动作进行探索，积累多样化的经验。随着训练的推进，$\varepsilon$逐渐降低，智能体越来越多地依赖已学到的Q值进行确定性决策。在探索阶段，系统还引入了一项偏向机制：10%的探索动作被强制设为"保持连接"（而非完全随机选择），这一设计旨在防止智能体在训练初期因过多的无效切换而陷入连接频繁中断的负面经验循环。

### 3.4.4 单智能体控制与混合管理架构

在多无人机场景下直接应用多智能体强化学习面临状态空间维度爆炸、信用分配困难和训练不稳定等挑战[4]。为规避这些难题，本系统采用了单智能体控制结合混合管理的架构设计。

具体而言，在RL环境中仅指定一架无人机（通常编号为0）由DQN智能体控制，其余所有无人机由增强启发式算法自动管理。这种架构具有三方面优势。第一，状态空间维度仅取决于单个无人机的观测信息，不随无人机总数的变化而增长，保证了训练的可行性。第二，非目标无人机由增强算法管理，确保了网络环境的稳定性和合理性，避免了多个学习中的智能体同时做出次优决策导致的训练环境退化。第三，DQN智能体可以在增强算法营造的"合理"网络环境中进行学习，而非在完全随机或由简单规则控制的环境中训练，这有助于智能体学到更具实用价值的策略。

RL环境通过包装器模式实现，将上一章设计的NetworkEnvironmentWithRecognition封装为标准的reset/step接口。在每个仿真步中，首先由DQN智能体为目标无人机选择并执行动作，然后由增强启发式算法为所有非目标无人机执行切换决策，最后统一执行环境步进（无人机移动、SINR更新、满意度记录等）。这种"先RL后启发式、最后统一步进"的执行顺序确保了RL智能体的决策和非RL无人机的管理在同一时间步内协调完成，避免了决策顺序对结果公平性的影响。

在训练过程中，为加速仿真执行，RL环境默认跳过业务识别过程，直接使用无人机的真实业务类型。这一简化不影响训练的正确性，因为RL智能体已经通过业务类型的one-hot编码获知了自身的业务信息，识别过程对决策结果没有影响。在评估阶段，可以启用完整的识别流程以验证RL策略在识别噪声存在时的鲁棒性。

## 3.5 三种算法的对比分析

### 3.5.1 决策机制对比

从决策机制的角度，三种算法呈现出从"规则驱动"到"效用驱动"再到"数据驱动"的演进路径。传统算法的决策逻辑完全由A3事件的固定公式所规定，输入为当前SINR值，输出为二值的切换/保持决策，决策过程透明且可预测，但灵活性极为有限。增强算法的决策过程由效用函数、动态阈值和降级搜索三个组件协同完成，输入为多维度网络状态（SINR、负载率、业务类型等），输出为（目标基站、降级比例）的组合决策，决策过程可解释且具备上下文感知能力。DQN算法的决策过程由神经网络参数化，输入为48维状态向量，输出为各动作的Q值估计，决策过程本质上是"黑箱"的，但具备从数据中自动学习复杂策略的潜力。

### 3.5.2 执行保障能力对比

在切换执行层面，三种算法的保障能力差异显著。传统算法采用"单次尝试、失败即断"的简单策略，没有任何降级或恢复机制。增强算法构建了"直接分配—抢占—回滚"三级保障链路，加上软迁移机制对被抢占无人机的后续处理，形成了完整的失败恢复体系。DQN环境中的切换执行则复用了增强算法的抢占和回滚机制，但在RL训练过程中，智能体需要通过试错自行学习何时切换、何时不切换，执行层面的保障机制为其提供了安全边界——即使智能体做出了不理想的切换决策，回滚机制仍能降低实际断连的概率。

### 3.5.3 计算复杂度对比

计算复杂度是实际部署中不可忽视的考量因素。传统算法的决策过程仅需遍历所有邻区基站一次，时间复杂度为$O(N_{bs})$，单次决策时间在微秒级别。增强算法需要对所有候选基站的所有降级比例计算效用值，时间复杂度为$O(N_{bs} \times N_{ratio})$，其中$N_{ratio}$为降级档位数量（最多5档），再加上动态阈值的计算和执行阶段的多级回退，单次决策时间约为0.15毫秒。DQN算法需要执行一次神经网络前向传播，时间复杂度取决于网络结构，单次决策时间与增强算法相当。虽然增强算法和DQN的决策时间约为传统算法的30倍，但绝对值均在亚毫秒级别，远低于5G网络中切换决策的典型时延预算（数十毫秒级），不会对系统的实时性构成实质影响。

## 3.6 本章小结

本章围绕无人机切换决策算法的设计与实现展开了系统论述。首先，设计并实现了基于3GPP A3事件触发机制的传统切换算法作为性能基线，分析了其在业务感知、资源利用和切换鲁棒性方面的固有局限。其次，针对上述局限，设计了一套包含业务感知效用函数、动态切换阈值、降级比例搜索、优先级抢占、回滚保护、软迁移和全局负载均衡七种增强机制的协同切换算法，构建了从决策到执行的完整多级保障体系。最后，将切换决策建模为马尔可夫决策过程，设计了包含48维状态空间和9维离散动作空间的DQN强化学习方法，并通过精心的奖励函数设计和单智能体混合管理架构确保了训练的稳定性和实用性。三种算法的设计在接口层面保持一致，为后续章节的系统性实验验证和对比分析奠定了算法基础。

## 参考文献

[1] 3GPP TS 38.331. NR Radio Resource Control (RRC) protocol specification[S]. Version 16.6.0, 2021.

[2] 3GPP TS 36.331. Evolved Universal Terrestrial Radio Access (E-UTRA) Radio Resource Control (RRC) protocol specification[S]. Version 15.10.0, 2020.

[3] V. Mnih, K. Kavukcuoglu, D. Silver, et al. Human-level control through deep reinforcement learning[J]. Nature, 2015, 518(7540): 529-533.

[4] K. Zhang, Z. Yang, H. Liu, et al. Fully decentralized multi-agent reinforcement learning with networked agents[C]. Proceedings of the 35th International Conference on Machine Learning (ICML), Stockholm, 2018: 5872-5881.

[5] S. Wang, H. Liu, P. H. J. Chong, et al. Machine learning based handover management in 5G ultra-dense networks[J]. IEEE Network, 2020, 34(5): 234-241.

[6] 3GPP TS 38.304. NR User Equipment (UE) procedures in idle mode and in RRC inactive state[S]. Version 16.5.0, 2021.

[7] Y. Zhao, Y. Li, H. Zhang, et al. Intelligent handover management in UAV-assisted cellular networks: A deep reinforcement learning approach[J]. IEEE Internet of Things Journal, 2022, 9(21): 21496-21509.

[8] R. S. Sutton, A. G. Barto. Reinforcement learning: An introduction (2nd edition)[M]. MIT Press, 2018.

[9] H. Van Hasselt, A. Guez, D. Silver. Deep reinforcement learning with double Q-learning[C]. Proceedings of the 30th AAAI Conference on Artificial Intelligence (AAAI), Phoenix, 2016: 2094-2100.
