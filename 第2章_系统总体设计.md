# 第2章 系统总体设计

## 2.1 系统设计目标与总体架构

随着无人机在物流配送、应急救援、农业监测、城市安防等领域的规模化部署，无人机与地面基站之间的通信切换问题日益凸显其重要性。在5G网络架构下，无人机用户与地面用户共享无线频谱资源，但两类用户在移动性、业务特征和服务质量需求方面存在显著差异。传统的切换决策机制普遍基于信号强度进行触发和目标选择，缺乏对业务类型和服务质量差异化需求的感知能力，难以适应多业务共存的无人机通信场景。基于上述背景，本章所设计的无人机业务识别与切换决策联动系统旨在构建一套从业务感知到切换决策再到效果评估的完整技术链路，实现对不同类型无人机业务的差异化服务质量保障。

系统设计的核心目标可以概括为以下三个方面。第一，建立业务识别与切换决策之间的联动机制，使切换算法能够根据无人机当前承载的业务类型做出差异化的切换决策，而非仅依赖信号强度指标进行统一的切换触发。第二，设计多维度、层次化的满意度评估体系，能够从速率、时延、丢包率等多个维度综合衡量网络对不同类型业务的服务质量，为算法的性能评估提供精细化指标。第三，在传统切换算法的基础上引入多种增强机制，包括业务感知效用函数、动态切换阈值、资源降级搜索、优先级抢占与回滚等，形成一套完整的多机制协同切换方案。

在总体架构层面，本系统采用模块化的分层设计思想，将整体架构划分为业务识别模块、切换决策模块和满意度评估模块三个核心功能层，各模块之间通过明确的接口进行数据交互与状态传递。业务识别模块负责实时采集无人机通信流的统计特征，利用机器学习模型对业务类型进行分类识别，并将识别结果连同置信度传递给切换决策模块。切换决策模块以识别结果为输入，结合当前网络状态信息，执行增强切换算法生成切换决策并完成切换执行。满意度评估模块则独立于切换决策过程，始终基于无人机真实的业务类型计算满意度指标，从而能够准确地反映识别错误对服务质量的影响，确保评估结果的客观性和可靠性。

这种"识别—决策—评估"的三层联动架构，使得系统能够形成从感知到执行再到反馈的闭环运作机制。特别值得指出的是，满意度评估模块使用真实业务类型而非识别结果进行计算这一设计，保证了评估过程不受识别误差的掩盖或放大，能够真实地呈现系统在各个层面的服务质量表现。这种设计在后续实验中将发挥重要作用，为业务识别准确性价值的定量验证提供了可靠的评估基础。

## 2.2 网络仿真环境设计

### 2.2.1 基站与无人机实体模型

为了对无人机通信场景进行系统化的仿真验证，本系统设计了基站和无人机两类核心网络实体，并对各自的属性与行为进行了抽象建模。

基站实体是网络侧的核心资源提供者，其属性包括基站标识、总容量、空间位置、基站类型（宏基站或微基站）、覆盖半径以及故障状态等。在容量设计方面，本系统参考5G宏基站和微基站的实际部署参数，宏基站采用100MHz带宽配置，下行容量约1Gbps，微基站下行容量约500Mbps，通过载波聚合和高阶MIMO技术可进一步提升至2Gbps以上。各基站维护一个已连接无人机列表，记录每个连接的无人机及其分配速率，并实时跟踪当前负载和可用容量。基站实体提供资源分配、资源释放和低优先级抢占等核心操作接口，其中抢占机制确保关键业务的无人机不会被低优先级用户挤占资源，这一设计将在后续增强切换算法中发挥关键作用。此外，基站还具备故障状态的模拟能力，当基站进入故障状态时，其可用容量归零，所有已连接的无人机将被强制断开，这一机制用于测试切换算法在突发故障场景下的鲁棒性。

无人机实体是网络侧的核心服务对象，其属性包括无人机标识、真实业务类型、当前识别业务类型、空间位置、三维速度向量、当前连接的基站标识、当前分配速率、信号与干扰加噪声比（SINR）、识别置信度以及满意度历史记录等。其中，真实业务类型在无人机初始化时确定，在整个仿真过程中保持不变，而识别业务类型则由业务识别模块动态更新。这种"真实类型不变、识别类型可变"的设计，使得系统能够在仿真过程中自然地模拟识别误差对切换决策和服务质量的影响。无人机实体的移动模型采用三维随机运动方式，在设定边界内以恒定速度运动并在碰到边界时反弹，不同应用场景下无人机速度的取值范围有所差异，以反映实际部署中各场景的移动性特征。例如，应急救援场景下无人机飞行速度较高，而城市监控场景下无人机多处于悬停或低速巡航状态。

在初始化阶段，每架无人机被随机分配为控制信令、视频回传或环境监测三种业务类型之一，分配比例根据具体应用场景进行配置。默认场景下三种业务的比例为控制信令40%、视频回传30%、环境监测30%。这种差异化的业务分布设计使得仿真环境能够反映真实场景中业务类型的异质性特征。

### 2.2.2 无线信道模型

无线信道模型是仿真环境中连接基站与无人机实体的物理层纽带，其核心功能是根据无人机与基站之间的空间距离计算信号质量指标。本系统采用的信道模型综合考虑了路径损耗和小尺度衰落两个因素。

在路径损耗方面，系统区分了城市和非城市两种典型部署环境。对于城市环境，采用3GPP推荐的Urban Macro（UMa）路径损耗模型，其表达式为：

$$PL = 140 + 38 \cdot \log_{10}(d/1000) \quad \text{(dB)}$$

其中，$d$为无人机与基站之间的距离，单位为米。对于非城市环境（如郊区、农业区域等），采用更为简化的路径损耗模型：

$$PL = 128.1 + 37.6 \cdot \log_{10}(d/1000) \quad \text{(dB)}$$

上述模型中，$d/1000$将距离转换为公里单位，与3GPP TR 36.814中推荐的宏基站路径损耗模型保持一致。两种模型的路径损耗指数分别约为3.8和3.76，符合文献中针对低空无人机信道实测得到的路径损耗指数范围[1]。

在小尺度衰落方面，系统采用Rayleigh衰落模型模拟多径效应引起的信号幅度波动。每对无人机-基站链路的Rayleigh衰落因子在每次仿真步更新时独立生成，以模拟信道的时间选择性特征。综合路径损耗和Rayleigh衰落后，SINR的计算公式为：

$$\text{SINR} = P_{tx} - PL + 10\log_{10}(h) - N_0$$

其中，$P_{tx}$为基站发射功率，$h$为Rayleigh衰落因子，$N_0$为噪声功率。系统设定$P_{tx}$为40dBm，$N_0$为-100dBm，这些参数参考了5G宏基站的典型发射功率配置[2]。

SINR矩阵在每个仿真步开始时根据所有无人机和基站的位置关系重新计算，确保信道状态的实时性。基于SINR值，系统进一步估算每架无人机的通信时延和丢包率，作为满意度评估模块的输入参数。

### 2.2.3 随机事件模型

在实际无人机通信场景中，网络状态不仅受无人机移动性的影响，还会受到基站故障、信道突发衰落、新无人机接入等随机事件的干扰。为了在仿真环境中充分模拟这些动态变化，本系统设计了四种类型的随机事件模型，并在增强网络环境中以一定概率在每个仿真步触发。

基站故障事件模拟基站因硬件异常或外部干扰导致的突发性服务中断。当触发该事件时，系统随机选取一个处于正常状态的基站将其标记为故障状态，该基站的可用容量立即归零，所有已连接的无人机被强制释放。基站恢复事件则是故障事件的逆过程，系统随机选取一个处于故障状态的基站进行恢复，使其重新具备服务能力。故障与恢复事件的配对设计使得仿真环境能够测试切换算法在基站生命周期变化场景下的适应能力。

信道突发衰落事件模拟因天气变化、电磁干扰或多径效应加剧导致的信道质量急剧恶化。当触发该事件时，系统随机选取一架无人机，对其与所有基站之间的SINR施加一个5至15dB的随机衰减值，模拟信道质量的临时性下降。这种设计能够测试切换算法在面对突发性信号质量下降时的快速响应能力。

新无人机到达事件模拟无人机群的动态扩容场景。当触发该事件时，系统在当前环境中新增一架随机业务类型的无人机，并为其分配随机的初始位置和速度。该事件使得仿真过程中的网络负载呈现动态增长趋势，能够更真实地反映实际部署中无人机数量不断变化的特征。

在默认配置下，随机事件的整体触发概率为每步5%，即平均每20个仿真步触发一次随机事件。四种事件类型的权重分配为：基站故障10%、信道突发衰落70%、新无人机到达15%、基站恢复5%。这一权重配置反映了实际网络中信道突发衰落是最为常见的干扰类型这一事实[3]。不同应用场景下，事件的触发概率和类型分布有所调整。例如，应急救援场景的动态性更高，事件触发概率提升至8%，且基站恢复事件的权重增加以模拟快速抢修机制；农业监测场景的环境相对稳定，事件触发概率降至2%。

## 2.3 业务类型识别模块

### 2.3.1 业务类型定义与特征空间

准确识别无人机当前承载的业务类型是实现业务感知切换决策的前提条件。本系统将无人机通信业务划分为三种典型类型：控制信令、视频回传和环境监测。这一分类体系与3GPP定义的5G三大应用场景——超可靠低时延通信（URLLC）、增强型移动宽带（eMBB）和海量机器类通信（mMTC）——形成了自然的对应关系[4]。

控制信令类业务对应URLLC场景，典型应用包括飞行控制指令的实时传输、避障信息交换等。该类业务具有低时延（最大允许时延20ms）、低带宽（理想速率0.5Mbps）、低丢包率（最大允许丢包率0.001%）的典型特征，在服务质量等级体系中处于最高优先级，关键程度达到1.0，时延敏感度为1.0。其QoS配置参考了3GPP TS 22.125中关于超高可靠通信的要求以及华为5G白皮书中的相关参数建议[5]。

视频回传类业务对应eMBB场景，典型应用包括实时视频流回传、4K巡检画面传输等。该类业务对带宽需求较高（理想速率50Mbps，最低保障速率25Mbps），同时要求较低的传输时延（最大允许时延20ms）和较低的丢包率。在服务质量等级体系中处于中等偏高的优先级，关键程度为0.7，时延敏感度为0.8。该类业务的参数设置参考了5G eMBB场景的典型性能指标[6]。

环境监测类业务对应mMTC场景，典型应用包括农田环境传感器数据采集、气象信息回传等。该类业务带宽需求较低（理想速率1Mbps），对时延的容忍度较高（最大允许时延1000ms），且能容忍一定程度的丢包（最大允许丢包率5%）。在服务质量等级体系中处于最低优先级，关键程度为0.3，时延敏感度仅为0.2。这类业务的参数反映了mMTC场景中"小数据包、大连接数、低频发送"的典型特征[7]。

在特征空间构建方面，系统为每架无人机提取四维流量特征向量，作为业务识别模型的输入。这四个特征维度分别为通信时延（单位：毫秒）、传输带宽（单位：Mbps）、丢包率和网络抖动（单位：毫秒）。三类业务在这四维特征空间中具有天然的分离度：控制信令类业务表现为低时延（均值10ms）、低带宽（均值0.5Mbps）、极低丢包率和低抖动；视频回传类业务表现为低时延（均值15ms）、高带宽（均值50Mbps）、低丢包率和中等抖动；环境监测类业务则表现为高时延（均值500ms）、低带宽（均值1Mbps）、较高丢包率和高抖动。具体而言，各类业务的特征参数由正态分布和Beta分布混合生成，并加入适当的高斯噪声以模拟真实网络环境中的测量不确定性。

三种业务在时延维度上呈现出明显的分层特征：控制信令的时延分布在10ms附近，视频回传在15ms附近，环境监测在500ms附近，三者均值之间相差一到两个数量级。在带宽维度上，视频回传（50Mbps）显著高于控制信令（0.5Mbps）和环境监测（1Mbps），形成了自然的二分聚类。这种多维特征空间中的天然分离度为分类模型的训练提供了良好的数据基础。

### 2.3.2 识别模型选型与训练

在确定特征空间后，系统的关键任务在于选择一种能够兼顾分类准确率和推理实时性的机器学习模型用于业务类型识别。为此，本系统设计了一套多目标优化模型选型机制，在五种候选分类算法中进行系统化评估和比较。

候选的五种算法分别为决策树（Decision Tree, DT）、支持向量机（Support Vector Machine, SVM）、多层感知机（Multi-Layer Perceptron, MLP）、随机森林（Random Forest, RF）和梯度提升决策树（Gradient Boosting Decision Tree, GBDT）。这些算法涵盖了传统统计学习方法、核方法和集成学习方法的代表，能够从多个角度验证特征空间的可分性。各模型的具体配置如下：决策树设置最大深度为12，最小分裂样本数为10，最小叶子节点样本数为5；SVM采用径向基核函数（RBF），正则化参数C=1.0，核函数系数采用自动缩放；MLP采用三层隐藏层结构，神经元数量分别为128、64、32，最大迭代次数1000次并启用早停策略以防止过拟合；随机森林包含100棵决策树，最大深度为15；GBDT包含100棵提升树，最大深度为5，学习率为0.1。

模型选型采用多目标优化评分机制，综合考量三个维度：分类准确性、模型稳定性和推理实时性。综合得分的计算公式为：

$$S = (0.4 \times F_1 + 0.3 \times CV_{mean} + 0.3 \times L_s) \times P$$

其中，$F_1$为测试集上的加权F1分数，$CV_{mean}$为五折交叉验证准确率的均值，$L_s$为延迟分数（定义为$\max(0, 1 - t_{infer}/10)$，$t_{infer}$为单样本推理延迟），$P$为延迟惩罚系数（当推理延迟超过10ms时取0.5，否则取1.0）。三个维度的权重分配体现了业务识别场景的核心诉求：准确性是基础（权重40%），稳定性关乎长期部署的可靠性（权重30%），实时性则直接影响切换决策的时效性（权重30%）。延迟惩罚系数的引入确保了推理速度过慢的模型即使在准确率上表现优秀也不会被选中。

训练数据采用合成生成方式，每个业务类别生成3000个样本，噪声水平设为0.1。数据集按80%训练集和20%测试集的比例划分，并采用分层抽样确保各类别在训练集和测试集中的比例一致。所有特征在进行模型训练前通过StandardScaler进行标准化处理，以消除不同特征维度之间的量纲差异。

经过系统化的训练与评估，多层感知机（MLP）模型在综合得分上表现最优，被选为系统的最终识别模型。MLP模型的测试集准确率达到97.71%，加权F1分数为0.9771，五折交叉验证均值准确率为97.53%（标准差仅为0.23%），单样本推理延迟仅为0.001ms，训练耗时0.62秒。从混淆矩阵来看，控制信令类业务几乎无误判（480个测试样本中仅有4个被误分为环境监测），视频回传类业务的误判率同样极低（480个样本中7个被误分为环境监测）。主要的误判出现在环境监测类业务中，有19个样本被误分为控制信令（占比3.96%），这可能是由于两类业务在带宽维度上的数值较为接近所致。

MLP模型之所以在此任务中表现最优，主要原因在于三类业务在时延-带宽特征空间中具有较强的天然分离度，而神经网络模型具有较强的非线性拟合能力，能够有效地学习到特征空间中的复杂决策边界。同时，MLP模型的推理延迟极低（0.001ms），远低于10ms的实时性阈值，完全满足无人机切换决策场景下对识别速度的要求。

### 2.3.3 自适应识别更新机制

在无人机实际飞行过程中，其承载的业务类型可能随任务阶段的变化而发生切换，例如从巡航阶段的环境监测业务切换至紧急避障阶段的控制信令业务。为了应对这种业务类型的动态变化，系统设计了自适应识别更新机制，通过控制识别频率和检测模型漂移来实现对业务类型变化的快速响应。

自适应识别更新器维护每个无人机的上次识别时间戳和更新历史，并引入三个核心控制参数：最小更新间隔、基础更新概率和漂移更新概率。最小更新间隔设为5个仿真步，即同一无人机两次识别之间至少间隔5步，以避免过于频繁的识别操作带来的计算开销。基础更新概率设为20%，即在满足时间间隔条件的前提下，每个仿真步有20%的概率执行识别更新。当检测到模型漂移时，更新概率提升至40%以加速识别频率。此外，当某架无人机的识别置信度低于0.8时，更新概率进一步提升至50%，以确保低置信度场景下的及时纠错。

模型漂移检测基于滑动窗口机制实现。更新器维护一个容量为100的反馈缓冲区，存储每次识别的结果（包括预测类型、真实类型、置信度和时间步）。当缓冲区中累积的反馈数量达到30条时，系统计算最近30次反馈中的识别错误率。若错误率超过25%的漂移阈值，则判定模型发生漂移并触发加速更新机制；当错误率回落至阈值以下时，漂移状态自动解除。这一机制的设计初衷是应对业务类型在仿真过程中发生变化的场景，但在当前仿真环境中由于业务类型保持静态不变，该机制的验证效果有限。这一限制将在后续的讨论中加以说明。

## 2.4 满意度评估模型

### 2.4.1 多维度满意度计算框架

满意度评估是衡量系统服务质量的核心量化指标。本系统突破了传统评估中仅依赖速率或仅依赖信号强度的单一维度局限，设计了涵盖速率、时延和丢包率三个维度的综合满意度计算框架。这一框架的建立基于一个基本认识：不同类型的业务对各项服务质量指标的关注程度存在本质差异，控制信令类业务对时延极为敏感而带宽需求较低，视频回传类业务则需要同时保障高带宽和低时延，环境监测类业务则对时延的容忍度很高但需要稳定的传输连接。因此，满意度计算必须能够根据业务类型自适应地调整各维度的权重。

满意度计算的整体流程可以概括为三个步骤。首先，分别计算速率满意度、时延满意度和丢包率满意度三个分项指标。其次，根据当前业务类型的时延敏感度参数确定三个分项指标的加权权重。最后，将加权后的分项满意度求和得到综合满意度。权重的分配遵循以下规则：时延敏感度越高，时延维度的权重越大，速率维度的权重相应降低（但设置0.4的下限以确保速率始终是重要的考量因素），丢包率维度的权重则随时延敏感度的提高而适度增加，反映高敏感度业务对传输质量的严格要求。

具体而言，速率满意度根据分配速率与理想速率的比值计算。控制信令类业务采用阶梯型函数，当速率比达到95%以上时满意度为1.0，低于70%时直接归零，中间区间分段线性插值。这种严格的设计反映了控制信令业务"要么满足、要么不可用"的二元特性。视频回传类业务采用Smoothstep平滑过渡函数，在速率比为0.7至0.9之间实现平滑过渡，避免因微小的速率波动导致满意度的剧烈变化，这符合视频流业务对带宽波动的容忍特性。环境监测类业务采用线性衰减函数，在速率比为0.3至0.8之间线性递增，体现了此类业务对速率变化的宽容态度。

时延满意度根据估计时延与最大允许时延的比值计算。当估计时延不超过最大允许时延时，时延满意度为1.0；当时延超出允许范围但在两倍以内时，满意度线性下降；当时延严重超出时，满意度趋于零。丢包率满意度的计算逻辑与时延满意度类似，根据实际丢包率与最大允许丢包率的关系确定。在综合满意度计算中，还引入了一项惩罚机制：当任一分项满意度低于0.2时，综合满意度乘以0.5的惩罚系数。这一设计确保了系统不会因为某个维度的极端恶化而被其他维度的良好表现所掩盖。

### 2.4.2 差异化满意度曲线

三类业务的差异化满意度曲线是本系统服务质量建模的核心特征之一，其设计充分体现了不同业务类型对资源分配的差异化需求。

控制信令类业务的满意度曲线呈现严格的阶梯型特征。这一设计源于控制信令业务的极端时延敏感性——飞行控制指令的传输延迟直接关系到飞行安全。当分配速率达到理想速率的95%及以上时，满意度为最大值1.0，表示服务质量完全满足要求。当速率比下降至85%至95%之间时，满意度快速降至0.7，表明服务质量开始出现可感知的退化。当速率比进一步下降至70%至85%之间时，满意度降至0.3。而当速率比低于70%时，满意度直接归零，意味着在此条件下控制信令业务已无法正常运作，继续维持连接已无实际意义。这种陡峭的满意度衰减曲线反映了URLLC场景下"硬性保障"的服务质量要求。

视频回传类业务的满意度曲线呈现平滑过渡型特征。在速率比为0.9以上时，满意度达到最大值1.0。在0.7至0.9之间，满意度通过Smoothstep函数（$3x^2 - 2x^3$）实现平滑过渡，从0.5渐变至1.0。Smoothstep函数的S形曲线在过渡区间的中点附近变化较缓，在两端附近变化较快，这一数学特性使得满意度在接近满速率时缓慢收敛至最优值，而在速率明显不足时加速下降，符合人眼对视频质量变化的主观感知规律。在0.5至0.7之间，满意度线性衰减至0.2。在0.5以下，满意度按比例缓慢衰减至零，但不会完全归零，体现了视频流业务在低速率下仍能维持基本可用性的特点。

环境监测类业务的满意度曲线呈现线性衰减型特征。在速率比达到0.8以上时满意度为1.0，在0.3至0.8之间线性递减至0.4，在0.3以下缓慢衰减至零但不完全归零。这一宽松的满意度曲线反映了环境监测业务对带宽波动的强容忍性——传感器数据通常以低频小包的形式发送，即使传输速率降低，仍能在较长时间内完成数据汇聚。三类业务的满意度曲线设计参考了3GPP TS 22.104中关于不同5G服务等级的服务质量要求以及相关文献中的业务体验质量建模方法[8]。

### 2.4.3 层次化网络满意度评估

在单无人机满意度计算的基础上，系统进一步设计了层次化的网络级满意度评估方法，从整体、加权、关键业务等多个视角全面刻画网络的服务质量表现。

整体满意度指标取所有无人机满意度值的算术平均值，反映网络对所有用户的平均服务质量水平。加权满意度指标按各业务类型的优先级进行加权平均，其中控制信令的权重为0.99，视频回传的权重为0.75，环境监测的权重为0.30。加权指标的设计使得高优先级业务的满意度对整体评分有更大的影响力，体现了网络资源分配应优先保障关键业务的系统设计理念。

关键业务满意度是一个二元指标，衡量关键业务（即关键程度不低于0.9的业务，在本系统中仅为控制信令）是否同时满足速率阈值和时延阈值要求。对于控制信令类无人机，只有当其分配速率不低于最低保障速率且估计时延低于最大允许时延时，该无人机的关键业务满意度才为1.0，否则为0.0。网络级的关键业务满意度取所有控制信令类无人机该指标的均值。这一指标的引入旨在从网络安全角度评估系统对最关键业务的保障能力。

此外，系统还提供按业务类型分组的满意度统计，能够独立呈现控制信令、视频回传和环境监测三类业务的各自满意度表现。这一分层统计能力使得分析者能够精准定位服务质量问题出在哪一类业务上，为算法的针对性优化提供数据支撑。

## 2.5 本章小结

本章围绕无人机业务识别与切换决策联动系统的总体设计方案，从网络仿真环境、业务识别模块和满意度评估模型三个层面展开了详细论述。首先，在网络仿真环境设计中，建立了基站和无人机两类核心实体的参数化模型，采用3GPP推荐的路径损耗模型结合Rayleigh衰落构建了符合5G低空通信特征的无线信道模型，并设计了包含基站故障、信道突发衰落、新无人机到达和基站恢复四种类型的随机事件模型以模拟真实部署环境的动态性。其次，在业务识别模块设计中，定义了控制信令、视频回传和环境监测三种典型业务类型及其QoS参数体系，构建了四维流量特征空间，并通过多目标优化评分机制在五种候选分类算法中选取了综合表现最优的MLP模型（准确率97.71%，推理延迟0.001ms），同时设计了自适应识别更新机制以应对业务类型的动态变化。最后，在满意度评估模型设计中，建立了涵盖速率、时延和丢包率三个维度的综合满意度计算框架，为三类业务分别设计了阶梯型、平滑型和线性型的差异化满意度曲线，并构建了层次化的网络级满意度评估体系。上述三个模块的设计为后续章节中切换算法的实现和实验验证奠定了坚实的技术基础。

## 参考文献

[1] 3GPP TR 36.814. Evolved Universal Terrestrial Radio Access (E-UTRA); Further advancements for E-UTRA physical layer aspects[S]. 2017.

[2] A. Ghosh, A. Maeder, M. Baker, et al. 5G evolution: A view on 5G cellular technology beyond 3GPP Release 15[J]. IEEE Access, 2019, 7: 127639-127651.

[3] W. Khawaja, I. Guvenc, D. W. Matolak, et al. A survey of air-to-ground propagation channel modeling for unmanned aerial vehicles[J]. IEEE Communications Surveys & Tutorials, 2019, 21(3): 2361-2391.

[4] 3GPP TS 22.261. Service requirements for the 5G system; Stage 1[S]. 2019.

[5] IMT-2020(5G)推进组. 5G场景和需求白皮书[R]. 2014.

[6] 3GPP TS 22.104. Service requirements for cyber-physical control applications in vertical domains[S]. 2021.

[7] 3GPP TS 22.125. Enhanced Mobile Broadband requirements[S]. 2018.

[8] 3GPP TS 22.104. Service requirements for cyber-physical control applications in vertical domains; Stage 1[S]. 2021.
