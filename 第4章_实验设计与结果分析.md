# 第4章 实验设计与结果分析

## 4.1 实验设计总体框架

在前述章节完成了系统总体架构设计、网络仿真环境搭建、业务识别模块与满意度评估模型构建以及三种层次化切换算法的设计与实现之后，本章通过系统化的实验对所提方案进行全面验证。实验设计的总体思路遵循"模块验证→系统验证→场景泛化"的递进逻辑，从识别模块的独立性能评估出发，逐步过渡到增强算法的机制有效性验证、两种算法的综合性能对比，最终在多个差异化应用场景和不同无人机密度条件下检验系统的泛化能力，并通过强化学习方法的引入探索数据驱动决策范式的可行性。

全部实验均基于第2章所设计的网络仿真环境执行，使用Python语言实现，依赖NumPy、PyTorch、SciPy和Matplotlib等科学计算与深度学习框架。为了保证实验结果的可靠性与可复现性，全局随机种子统一设为42，所有涉及随机过程的模块（包括无人机位置初始化、移动模型、信道衰落、业务分配以及DQN训练中的探索策略等）均受该种子控制。每个实验条件至少重复运行6至10次独立试验，取均值和标准差作为最终结果报告，并辅以统计显著性检验以确认性能差异的可靠性。

实验环境的基本配置如下：仿真区域为1000m×1000m×200m的三维空间，基站数量为8个，无人机数量根据具体实验设定在10至40架之间，每个仿真episode包含150个时间步。三类业务——控制信令、视频回传和环境监测——按照场景差异化比例分配。基站容量参考5G宏基站实际参数，容量范围根据实验需要设为（400, 800）Mbps或（250, 450）Mbps两个区间，分别对应资源充裕和资源紧张的仿真条件。下表汇总了实验参数的核心配置。

表4-1 实验核心参数配置

| 参数名称 | 取值 | 说明 |
|---------|------|------|
| 仿真区域 | 1000m×1000m×200m | 三维空间 |
| 基站数量 | 8 | 含宏基站与微基站 |
| 无人机数量 | 10/20/30/40 | 按实验梯度设定 |
| 仿真步数 | 150步/episode | 每次独立试验时长 |
| 重复次数 | 6-10次 | 取均值±标准差 |
| 全局随机种子 | 42 | 保证可复现性 |
| DQN训练轮次 | 1000 episodes | 含ε贪心探索衰减 |
| DQN网络结构 | 128-128全连接 | 输入48维/输出9维 |
| 学习率 | 5×10⁻⁴ | Adam优化器 |
| 折扣因子γ | 0.95 | 中等时序依赖 |
| 经验回放容量 | 50000 | 足够的状态-动作对存储 |
| 批量大小 | 64 | 每步训练采样数 |

本章共设计了五组实验，各实验之间的逻辑关系和验证目标如下所述。实验一聚焦于业务识别模块的独立性能评估，通过人为控制识别准确率来量化识别精度对系统整体满意度的影响。实验二和实验二b采用消融实验的方法，逐步验证增强算法中各机制对系统性能的独立贡献与协同效果。实验三在标准仿真条件下对增强算法与传统算法进行全面对比，从切换成功率、满意度、负载均衡等多个维度进行量化评估。实验四将对比扩展至五个差异化应用场景，检验系统的场景泛化能力。实验五引入基于深度强化学习的DQN切换决策方法，在不同无人机密度条件下与两种启发式算法进行对比，并通过特征消融实验验证DQN状态空间各设计要素的有效性。

## 4.2 业务识别准确性的价值验证

### 4.2.1 实验设计

实验一的核心验证目标是回答以下问题：业务识别准确率的提升对无人机切换决策系统整体服务质量的影响有多大？这一问题的重要性在于，如果识别准确率的变化对系统性能的影响微乎其微，那么在识别模块上投入更多计算和设计资源将缺乏合理性。反之，若识别准确率的改善能够带来系统性能的显著提升，则业务感知切换决策框架的价值便得到了实证支撑。

实验设计了五个识别准确率梯度条件，采用人工注入噪声的方式控制识别模块的输出质量。具体而言，条件A（Perfect）使用100%准确率，即识别结果始终与真实业务类型一致，代表理论上限基准。条件B（High）将准确率控制在90%左右，模拟高质量识别模型的输出。条件C（Medium）将准确率控制在80%左右，模拟中等质量模型的表现。条件D（Low）将准确率降至60%左右，模拟低质量模型或复杂环境下的识别退化。条件E（Random）采用随机分配策略，准确率约为33%（三类业务的均匀猜测概率），代表无识别能力的下界对照。所有条件均使用增强切换算法，控制变量一致，仅改变识别准确率这一自变量。每组条件运行10次独立试验，每次试验150步，使用8基站20架无人机的标准配置。

### 4.2.2 实验结果与分析

实验一的各项性能指标随识别准确率的变化呈现出清晰的单调递增趋势，验证了业务识别准确性对系统整体性能的决定性影响。在整体满意度方面，当识别准确率从33%（随机条件）提升至100%（完美条件）时，整体满意度从0.746显著提升至0.959，相对提升幅度达28.6%。即使在从80%到90%的高准确率区间内，满意度也从0.893提升至0.936，仍有约4.8%的增益。这一结果表明，即使在识别准确率已经较高的条件下，进一步优化识别模型仍能带来可观的系统性能改善，这为持续投入识别模块的研究与优化提供了实证依据。

资源匹配度指标对识别准确率的变化尤为敏感，呈现出近似线性的响应关系。在完美条件下，资源匹配度仅为0.954（均值），而当准确率降至80%时，资源匹配度迅速攀升至6.16。在随机条件下，该指标更是高达17.11。资源匹配度的物理含义是各无人机实际分配到的速率与最优速率之间的偏差的某种度量指标，其值越低表示资源分配越合理。该指标的大幅恶化表明，当识别模块将视频回传类无人机误识别为环境监测类时，切换算法会为其分配远低于实际需求的带宽资源，造成严重的资源错配。反之，当环境监测类无人机被误识别为视频回传类时，算法可能为其执行不必要的切换或分配过多的资源，挤占了其他无人机的可用容量。

关键业务满意度是本系统设计中最受关注的指标之一，其实验结果尤为值得分析。在完美识别条件下，关键业务（控制信令类）满意度高达0.997，几乎接近理论最优值。当准确率降至60%时，该指标降至0.917，下降了8个百分点。而在随机识别条件下，关键业务满意度降至0.874。需要指出的是，即使识别准确率下降，关键业务满意度的绝对值仍然维持在较高水平（0.874以上），这得益于增强算法中优先级抢占机制的存在——高优先级业务在资源竞争中被优先保障。然而，这一保障机制的有效性依赖于识别模块能够正确地将控制信令类无人机识别出来，一旦发生误判，抢占机制便无法被正确触发，这正是识别准确率下降导致关键业务满意度降低的根本原因。

加权满意度指标综合考虑了业务优先级因素，给予控制信令类业务更高的评价权重。在完美识别条件下，加权满意度为0.681，而在随机条件下仅为0.419，相对下降了38.5%。这一显著的差距直观地反映了识别准确率对差异化服务质量保障体系有效性的影响：只有当识别模块能够准确区分业务类型时，切换算法才能正确地执行差异化的资源分配和切换策略，从而真正实现"关键业务优先保障"的设计初衷。

在切换成功率方面，五个条件下的表现均在0.97以上，差异相对较小。这一结果表明，切换成功率主要受切换执行机制（如降级搜索、回滚保护等）的影响，而识别准确率对其的直接影响有限。然而，切换成功率的趋同并不意味着识别准确率不重要——在切换成功的前提下，不同条件下的服务质量存在显著差异，这恰恰说明了"切换成功"与"切换到正确的基站并获得足够的资源"是两个不同层次的问题。

图4-1（建议自绘）展示了各项指标随识别准确率变化的趋势曲线，可以清晰观察到各指标的单调递增关系以及不同指标对识别准确率的敏感度差异。其中，资源匹配度对准确率变化最为敏感（斜率最大），加权满意度次之，切换成功率的敏感度最低。这一发现为系统优化提供了方向性指导：在资源受限的场景下，提升识别准确率能够带来最为显著的资源分配改善。

## 4.3 增强算法机制有效性验证

### 4.3.1 单机制消融实验

实验二的核心目标是验证增强算法中各个独立机制对系统性能的贡献。增强算法的设计并非单一机制的简单叠加，而是在决策层面和执行层面引入了多项协同工作的机制。为了厘清各机制的独立贡献，本实验采用逐步添加机制的方法，从传统算法基线出发，依次引入动态切换阈值、业务权重差异化、ε贪心探索、全局负载均衡和自适应识别更新等机制，比较每一步引入后系统性能的变化。

实验二的基线为传统3GPP A3算法，其平均满意度为0.648，切换成功率仅为0.804，关键业务满意度为0.919。传统算法的负载方差高达0.079，反映出严重的负载不均衡问题，部分基站过载而部分基站资源空闲。

引入动态切换阈值机制后，系统性能出现了显著改善。平均满意度从0.648提升至0.736，提升了13.6%，切换成功率从0.804跃升至0.960，提升了19.4%。负载方差从0.079大幅降至0.006，下降了92.8%。这一结果充分验证了动态切换阈值机制的有效性。其根本原因在于，动态阈值根据无人机的业务类型、当前基站负载和移动性特征自适应调整切换触发条件，使得高优先级业务在信道质量初期恶化时即提前切换，避免了因等待A3条件满足而错失最佳切换时机的风险。同时，负载因子的引入使得处于高负载基站上的无人机更积极地寻找替代基站，从宏观上促进了负载均衡。

在动态阈值基础上引入业务权重差异化后，切换成功率进一步提升至0.985，负载方差进一步降至0.004。平均满意度维持在0.736附近，基本不变。业务权重差异化对切换成功率的贡献源于效用函数中权重的差异化设计——通过为不同业务类型设定不同的速率、负载和信号质量权重，效用函数能够更准确地评估各候选基站对不同类型业务的服务能力，从而做出更优的切换目标选择。

引入ε贪心探索机制后，切换成功率出现了一定程度的波动（从0.985降至0.933），同时标准差增大（从0.021升至0.168），这表明随机探索在提升切换决策多样性的同时也引入了不稳定性。然而，从加权满意度角度看，引入ε贪心后该指标从0.436提升至0.438，总负载从4432提升至4666，说明探索机制在特定情况下能够帮助无人机发现更优的基站资源，但在本实验的仿真条件下，这种收益并不稳定。这一发现对于理解后续DQN方法中探索-利用权衡的设计具有重要意义。

引入全局负载均衡机制后，总负载从4398提升至4483Mbps，系统整体的资源利用率得到改善。切换成功率回升至0.967，消除了ε贪心引入的不稳定性。全局负载均衡机制通过周期性检查各基站的负载状况，将过载基站上的低优先级无人机迁移至负载较低的基站，从宏观层面优化了网络资源分配。

将所有机制组合为完整增强算法后，系统各项指标达到了综合最优状态：平均满意度0.737，切换成功率0.967，关键业务满意度0.962，加权满意度0.436，总负载4483Mbps，负载方差0.005。与传统基线相比，完整增强算法在切换成功率上提升了20.1%，在负载均衡度上改善了93.4%，在整体满意度上提升了13.7%。

### 4.3.2 多机制协同消融实验

实验二b在实验二的基础上进一步细化，通过枚举不同机制的组合来验证多机制协同效果。该实验共设计了16种机制组合（含传统基线和完整增强算法），覆盖了动态阈值、业务权重、ε贪心和负载均衡四个机制的2⁴=16种开关组合。

实验结果揭示了若干有价值的发现。首先，动态切换阈值是性能贡献最大的单一机制，无论与其他机制如何组合，包含动态阈值的组合在切换成功率上均显著优于不含动态阈值的组合。在仅含动态阈值和业务权重两个机制的组合中（dyn_thresh_weights），切换成功率达到0.982，负载方差仅为0.002，几乎与完整增强算法持平。这一结果表明，动态阈值与业务权重的组合已经能够覆盖增强算法绝大部分的性能增益。

其次，多机制并非总是产生正向协同效果。当四个机制全部开启时（dyn_thresh_weights_epsilon_lb），部分指标反而低于三个机制的最优子集。例如，四机制全开的切换成功率为0.960，低于三机制组合（dyn_thresh_weights_lb）的0.982。这一"负协同"现象的原因在于，过多的机制同时作用可能产生策略冲突——ε贪心鼓励探索切换目标，而负载均衡可能同时触发迁移，两者叠加可能导致不必要的切换操作，反而增加了切换失败的风险。这一发现提示在工程实现中需要审慎设计机制之间的优先级和互斥逻辑。

第三，在加权满意度这一综合性指标上，不同组合之间的差异相对较小（0.429至0.442），而在切换成功率和负载方差上的差异更为显著。这表明增强算法各项机制的主要贡献在于提高切换执行的鲁棒性和资源分配的均衡性，而非直接改变满意度计算的输入参数。换言之，机制优化的效果主要体现在"过程"层面（更少的切换失败、更均衡的负载），而非"结果"层面（满意度值本身），但过程层面的改善通过减少断连和资源饥饿事件间接提升了整体满意度。

### 4.3.3 核心机制与完整机制对比实验

实验二c进一步聚焦于区分增强算法中的"核心机制"和"附加机制"。核心机制定义为动态切换阈值与业务权重差异化的组合，附加机制包括ε贪心探索和全局负载均衡。该实验通过四组对比（核心无探索、核心有探索、完整无探索、完整有探索）来量化附加机制的价值。

实验结果（6次独立试验，200步/episode）显示，核心机制的四组配置均实现了100%的切换成功率，显著高于传统算法的78%-80%水平。核心无探索组的平均满意度为0.747，完整有探索组为0.756，附加机制带来的额外提升约为1.2%。在加权满意度方面，核心无探索组为0.441，完整无探索组为0.459，附加机制的贡献约为4.1%。

负载方差的分析结果颇具启发意义。核心机制的负载方差约为0.010-0.021，而完整机制的负载方差增至0.033-0.036。这一看似矛盾的结果（完整机制的负载均衡反而更差）实际上反映了全局负载均衡机制的双刃剑效应：虽然负载均衡从宏观上优化了资源分配，但迁移操作本身会产生短期的负载波动，在200步的较长仿真周期内，这种波动被更充分地暴露出来。这一发现表明，负载均衡机制的优化频率和迁移粒度需要根据具体应用场景进行调优。

综合三组消融实验的结果，可以得出以下结论。动态切换阈值是增强算法中最为关键的单一机制，对切换成功率和负载均衡的贡献最大。业务权重差异化在动态阈值的基础上进一步优化了切换目标选择的质量。ε贪心探索的价值在当前仿真条件下有限，且引入了不稳定性，但在DQN强化学习方法中，探索机制是模型学习有效策略的必要条件。全局负载均衡能够提升系统整体的资源利用率，但需要合理控制迁移频率以避免短期波动。四组机制的最优组合并非简单地全部开启，而是需要根据具体场景的资源充裕程度和业务分布特征进行适应性调整。

## 4.4 增强算法与传统算法的综合性能对比

### 4.4.1 实验设置

实验三是本论文最核心的对比实验，旨在标准仿真条件下对增强算法与传统算法进行全面、量化的性能比较。实验采用8基站20架无人机的标准配置，运行10次独立试验。评估指标涵盖切换成功率、平均满意度、关键业务满意度、加权满意度、切换时延、决策时间、总负载、负载方差、连接保持率等多个维度，并辅以统计显著性检验（独立样本t检验或Mann-Whitney U检验，根据数据正态性自动选择）以确认差异的统计可靠性。

### 4.4.2 切换性能对比

增强算法在切换成功率方面表现出显著优势，平均成功率达到98.07%，较传统算法的84.41%提升了16.19个百分点。统计检验结果表明，这一差异具有高度统计显著性（p=0.0004，Mann-Whitney U检验），效应量（effect size）为0.794，属于大效应。传统算法切换成功率较低的根本原因在于其"先断后连"的执行策略——一旦释放旧基站资源后目标基站分配失败，无人机便直接进入断连状态，没有任何回退手段。而增强算法通过降级比例搜索、优先级抢占、回滚保护和软迁移等多级保障机制，为切换执行提供了多条备选路径，大幅降低了切换失败的概率。

在切换时延方面，增强算法的平均切换时延（0.0029ms）略高于传统算法（0.0016ms），这是因为增强算法需要执行效用计算、降级搜索和资源协商等额外操作，增加了决策计算开销。但从绝对值来看，两者的时延差异仅为微秒级别，在实际系统中完全可以忽略不计。值得注意的是，增强算法的决策时间（0.148ms）显著高于传统算法（0.005ms），这是因为增强算法的效用函数涉及多维度的权重计算和归一化操作。然而，0.148ms的决策时间仍远低于5G网络中切换准备的典型时间尺度（数十毫秒），因此在实时性方面不存在瓶颈。

传统算法存在19.96%的"错失机会率"（missed opportunity rate），意味着近五分之一的本应执行切换的场景因A3条件未满足而被错过。增强算法通过动态阈值机制将这一比率降至0%，确保所有有利的切换机会均被有效利用。

### 4.4.3 满意度与服务质量对比

增强算法的平均满意度为0.739，较传统算法的0.674提升了9.63%。统计检验确认该差异具有显著性（p=0.008，t检验），效应量为1.408，属于大效应。关键业务满意度方面，增强算法为0.960，传统算法为0.944，提升了1.70%。虽然提升幅度相对较小，但关键业务满意度的绝对值在两种算法中均维持在较高水平，这反映了基站容量在当前仿真配置下尚能基本满足关键业务的需求。

加权满意度的对比结果更具说服力。增强算法的加权满意度为0.436，传统算法为0.398，提升了9.54%。加权满意度通过赋予控制信令类业务更高的评价权重，更能反映差异化服务质量保障的实际效果。加权满意度的显著提升表明，增强算法在保障关键业务方面的优势不仅体现在平均满意度上，更体现在对高优先级业务的定向保护上。

在分维度满意度方面，增强算法的速率满意度为0.683，传统算法为0.601，提升了13.65%。这一提升主要得益于增强算法中速率匹配度评估机制的存在——效用函数中的速率权重使得算法倾向于选择能够提供更多可用带宽的基站，即使在信号质量略逊的情况下。时延满意度方面，两种算法差异较小（增强0.678 vs 传统0.676），这是因为时延满意度主要受切换中断次数的影响，而两者在仿真环境中的时延模型相同。

### 4.4.4 负载均衡与资源利用对比

负载均衡方面的差异最为显著。增强算法的负载方差为0.004，传统算法为0.072，增强了94.4%（负载方差降低了94.4%）。统计检验结果（p=1.09×10⁻⁸，效应量-4.652）表明这一差异具有极高的统计显著性。传统算法的高负载方差源于其纯SINR驱动的切换策略——大量无人机倾向于切换到SINR最高的少数几个基站，导致这些基站过载而其他基站资源闲置。增强算法通过效用函数中的负载率权重和全局负载均衡机制，有效抑制了这种资源聚集效应。

总负载方面，增强算法为4412Mbps，传统算法为3730Mbps，提升了18.3%（p=0.002，效应量1.680）。总负载的提升反映了增强算法在资源利用率上的优势——通过更均衡的资源分配和更鲁棒的切换执行，更多的无人机能够成功连接并获得服务，系统整体的吞吐量随之提升。

连接保持率方面，增强算法为94.69%，传统算法为88.68%，提升了6.78个百分点。连接保持率的提升直接受益于切换成功率的改善——更少的切换失败意味着更少的断连事件，无人机能够维持更稳定的基站连接。

### 4.4.5 统计显著性分析

为全面评估各项指标差异的统计可靠性，本实验对所有核心指标进行了假设检验。检验方法根据数据的正态性自动选择：Shapiro-Wilk检验用于判断数据是否符合正态分布，若两组数据均服从正态分布则采用独立样本t检验，否则采用Mann-Whitney U检验。显著性水平设为α=0.05。

在所检验的7项核心指标中，6项指标的差异达到了统计显著性。其中，平均满意度（p=0.008）、切换成功率（p=0.0004）、总负载（p=0.002）和负载方差（p<0.001）的差异高度显著。决策时间和切换时延的差异虽然也达到显著性水平，但如前所述，其绝对值差异在工程上可忽略不计。唯一未达到显著性的指标是关键业务满意度（p=0.364），效应量为0.211（小效应），这与前述分析一致——在当前仿真配置下，关键业务满意度在两种算法中均已接近饱和，差异空间有限。

## 4.5 多场景泛化能力验证

### 4.5.1 场景配置

实验四将增强算法与传统算法的对比扩展至五个差异化应用场景，以验证系统的场景泛化能力。五个场景的设计参考了当前无人机通信的主要应用领域，各自在无人机密度、业务分布、移动性特征和资源需求方面具有显著差异。

智慧城市监控场景配置15架无人机，以视频回传类业务为主（占比约50%），无人机速度较低（5-15m/s），模拟城市上空悬停或低速巡航的监控无人机集群。工业巡检场景配置12架无人机，以控制信令类业务为主（占比约60%），需要低时延高可靠的通信保障，无人机速度适中（10-25m/s）。农业监测场景配置25架无人机，以环境监测类业务为主（占比约55%），覆盖面积大，无人机速度快（15-30m/s），单次巡检时间较长。应急救援场景配置8架无人机，业务类型均匀分布，无人机速度最快（25-40m/s），模拟灾区上空快速侦察的通信需求。物流配送场景配置18架无人机，业务类型较为混合，无人机速度中等（10-20m/s），路径相对固定。每个场景运行10次独立试验，每次150步。

### 4.5.2 各场景结果分析

在智慧城市监控场景中，增强算法的整体满意度为0.511，传统算法为0.508，两者差异较小（仅0.6%）。这一结果可以归因于该场景的低密度和相对充裕的资源条件——15架无人机在8个基站上的平均密度仅为1.9架/基站，资源竞争压力较小，两种算法均能为大多数无人机提供充足的服务。尽管如此，增强算法在切换成功率上仍保持100%的优势（传统算法为95.5%），加权满意度也略高于传统算法（0.292 vs 0.287）。负载方差方面，增强算法（0.025）显著低于传统算法（0.053），表明即使在资源充裕条件下，增强算法的负载均衡能力仍然具有优势。

工业巡检场景的结果与智慧城市场景类似，增强算法的整体满意度（0.404）略高于传统算法（0.389），切换成功率维持100%（传统算法为90.5%）。值得关注的是，该场景下增强算法的关键业务满意度高达0.987，显著高于传统算法的0.975。工业巡检场景以控制信令类业务为主，对通信可靠性要求极高，增强算法通过优先级抢占机制为关键业务提供了更强的保障。总负载方面，增强算法为1881Mbps，高于传统算法的1728Mbps，表明增强算法在资源利用效率上具有优势。

农业监测场景是最能体现增强算法优势的场景之一。该场景配置25架无人机（约3.1架/基站），资源竞争压力明显增大。增强算法的整体满意度达到0.690，较传统算法的0.474提升了45.6%，这一提升幅度远超其他场景。切换成功率方面出现了有趣的现象：增强算法的切换成功率仅为24.8%，远低于传统算法的73.2%。这一看似矛盾的结果需要结合负载方差来理解——增强算法的负载方差仅为0.0002，而传统算法高达0.137。增强算法在农业监测场景中通过全局负载均衡和动态阈值机制，将资源分配调整到接近最优状态，使得大部分无人机在当前基站上已经获得了接近最优的服务，无需触发切换。因此，切换次数的减少并非算法缺陷，而是系统达到良好均衡状态的自然结果。与之相对，传统算法因为频繁的不必要切换（触发A3条件但切换后服务质量并未改善），反而产生了更多的切换操作和更高的失败率。总负载方面，增强算法（5625Mbps）大幅高于传统算法（3769Mbps），表明增强算法在资源受限条件下能够显著提升系统整体的资源利用效率。

应急救援场景展示了增强算法在高移动性条件下的优势。增强算法的整体满意度为0.894，传统算法为0.871，提升了2.6%。切换成功率方面，增强算法维持100%（传统算法为95.3%）。在该场景中，无人机飞行速度快（25-40m/s），切换窗口较窄，传统算法的固定迟滞参数可能导致错过最佳切换时机。增强算法的动态阈值机制通过移动性因子降低了高速移动无人机的切换门槛，使其能够在信号质量开始恶化时即提前切换，避免了因来不及切换而导致的通信中断。加权满意度方面，增强算法为0.765，传统算法为0.738，提升了3.7%。

物流配送场景的结果综合体现了增强算法的各项优势。整体满意度（0.657 vs 0.642，提升2.4%）、切换成功率（100% vs 0.942）、关键业务满意度（0.949 vs 0.920，提升3.2%）和加权满意度（0.470 vs 0.441，提升6.6%）等指标均呈现一致性提升。负载方差方面，增强算法（0.026）低于传统算法（0.102），表明在物流配送场景这种业务类型混合、无人机密度中等的条件下，增强算法的负载均衡机制仍然有效运作。

### 4.5.3 跨场景对比总结

综合五个场景的实验结果，可以总结出以下规律。第一，增强算法在所有场景中的切换成功率均不低于传统算法，在三个场景（工业巡检、应急救援、物流配送）中达到100%，验证了增强算法的切换鲁棒性不受应用场景限制。第二，增强算法在资源受限场景（农业监测）中的优势最为显著，整体满意度提升达45.6%，这表明增强算法的核心价值在于提升资源紧张条件下的服务质量。第三，在资源充裕场景（智慧城市、工业巡检）中，增强算法的优势相对温和，主要体现在切换成功率和负载均衡方面，整体满意度的提升空间因资源充足而受到自然限制。第四，增强算法的负载均衡能力在所有场景中均表现一致，负载方差始终低于传统算法，验证了全局负载均衡机制的普适性。第五，关键业务满意度在多数场景中保持较高水平（0.949以上），这得益于优先级抢占机制的保护作用，但在应急救援场景中，关键业务满意度出现了异常下降（0.911），这可能是因为该场景下无人机高速移动导致的频繁断连事件中包含部分关键业务无人机。

## 4.6 基于深度强化学习的切换决策探索

### 4.6.1 DQN训练过程分析

实验五在前述启发式算法对比的基础上，引入基于深度Q网络（Deep Q-Network, DQN）的强化学习方法，探索数据驱动决策范式在无人机切换场景中的可行性与潜力。DQN方法的核心思想是让智能体通过与环境的反复交互，自主学习最优的切换策略，而无需人工设计效用函数或切换阈值等启发式规则。

DQN的训练配置如下：输入状态空间为48维，输出动作为9维（1个"保持"动作加8个"切换至基站i"动作），网络结构采用两个128神经元的全连接隐藏层，使用ReLU激活函数和Huber Loss，目标网络通过软更新（Soft Update）机制每500步同步一次。经验回放缓冲区容量为50000，批量训练大小为64。探索策略采用ε贪心机制，ε从1.0线性衰减至0.01，衰减因子为0.995。每个场景训练1000个episode，每个episode包含150步。

DQN训练过程中同时监测奖励值、满意度、损失值和ε衰减四个指标。在典型训练过程中，前200个episode的奖励值波动较大，智能体处于以探索为主的阶段。随着ε逐渐降低，奖励值的滑动平均值开始稳步上升，表明智能体逐步学习到了有效的切换策略。满意度曲线的变化趋势与奖励值基本一致，在训练后期稳定在0.95-1.0区间。损失值在训练初期较高（约0.5-1.0），随着Q值估计逐步收敛而下降至0.05以下。ε在约500个episode后降至0.1以下，智能体从探索为主转向利用为主。

需要指出的是，不同无人机密度场景下的DQN训练收敛速度和最终性能存在差异。在10架无人机的低密度场景中，资源充裕，智能体几乎不需要执行切换即可维持高满意度，学习任务较为简单，训练收敛较快。而在30和40架无人机的高密度场景中，资源竞争激烈，切换决策的优劣对满意度影响显著，学习难度增大，需要更多的训练轮次才能收敛。

### 4.6.2 多密度场景算法对比

实验五的核心对比在三种算法之间展开：传统算法（3GPP A3基线）、增强启发式算法和DQN强化学习方法。对比在四种无人机密度条件下（10、20、30、40架）分别执行，每种条件重复10次独立试验。

在10架无人机的低密度场景中，三种算法的性能差异较小。传统算法和增强算法的平均满意度均接近1.0（资源充裕条件下所有无人机都能获得充足服务），DQN的平均满意度约为0.997。DQN在极少数时间步上出现了满意度下降（最低至0.38），这对应于DQN选择切换但目标基站分配失败的情况。尽管如此，DQN的整体表现与启发式算法相当，连接保持率维持在100%。在切换次数方面，传统算法和增强算法的切换次数约为28次/episode，而DQN仅执行了约4次切换。DQN的"保守切换"策略在资源充裕场景中是合理的——既然当前基站已能提供充足服务，保持连接不动是更优的选择，这避免了不必要的切换操作和潜在的切换中断风险。

在20架无人机的中等密度场景中，三种算法的性能差距开始显现。增强算法和DQN在平均满意度和连接保持率上均优于传统算法。DQN展现出与增强算法相当的整体性能，但在切换策略上存在差异：DQN倾向于执行更少的切换操作，更多时间保持当前连接，而增强算法则在更积极地寻找更优基站。这种策略差异反映了两种方法的设计哲学不同：DQN通过学习选择了"稳定优先"的策略，而增强算法通过启发式规则鼓励"优化导向"的切换。

在30架无人机的高密度场景中，资源竞争明显加剧（约3.75架/基站），三种算法的性能差异进一步扩大。传统算法因频繁的切换失败而导致大量无人机断连，整体性能显著下降。增强算法通过降级搜索和抢占机制在资源紧张条件下仍能维持较高比例的成功切换。DQN在该场景中面临更大的学习挑战，其性能表现取决于训练是否充分以及状态空间设计是否捕捉到了关键的资源竞争信息。

在40架无人机的极高密度场景中，资源竞争最为激烈（5架/基站），系统进入了严重的过载状态。在该条件下，增强算法的负载均衡机制发挥了关键作用，通过周期性的负载再分配避免了单点过载。DQN在极高密度条件下的表现受限于其单智能体控制范围——DQN仅控制一架目标无人机的切换决策，无法像增强算法那样进行全局负载调控。这一设计差异在资源充裕条件下影响不大，但在资源严重不足时会成为限制因素。

### 4.6.3 DQN状态空间消融实验

为验证DQN状态空间中各设计要素的贡献，实验五的第二阶段在30架无人机场景下进行了特征消融实验。DQN的完整状态空间包含48维特征，其中核心设计要素包括：各基站的容量比值特征（反映资源竞争强度）、全局资源紧张度指标（反映系统整体资源充裕程度）和自适应切换惩罚（根据系统负载动态调整切换失败惩罚力度）。

消融实验设计了四个消融组，分别移除上述特征中的一个或全部，与完整模型（Full）进行对比。消融组的训练配置与完整模型一致（1000 episodes），每组评估10次以获得可靠的统计结果。

完整模型（Full）在消融实验中作为基准，其核心指标如下：平均满意度0.756，连接保持率100%，切换次数与增强算法相近。移除容量比值特征（-capacity_ratios）后，DQN失去了对各基站资源竞争强度的直接感知能力，可能导致智能体将切换决策过于依赖信号质量而忽视目标基站的可用容量。移除全局资源紧张度特征（-global_tension）后，DQN无法感知系统整体层面的资源充裕程度，可能在资源严重不足时仍频繁尝试切换，增加了切换失败的概率。移除自适应切换惩罚（-adaptive_penalty）后，奖励函数中的切换惩罚项变为固定值，无法根据负载情况动态调整惩罚力度，在资源紧张条件下可能导致过度惩罚（抑制了必要的切换）或惩罚不足（鼓励了不必要的切换）。

将三个特征同时移除（-全部）后，DQN退化为一个仅依赖基本信道信息和无人机自身状态的基础版本。该版本虽然在资源充裕条件下也能达到较高性能，但在资源竞争激烈的条件下面临更大的性能退化风险，验证了上述三个增强特征对DQN决策质量的正向贡献。

综合消融实验的结果表明，DQN状态空间的设计并非简单地堆砌所有可用信息，而是需要精心选择和构造能够反映切换决策关键影响因素的特征。容量比值、全局资源紧张度和自适应切换惩罚三个特征分别从微观（单基站）、宏观（全网）和策略（奖励塑造）三个层面丰富了DQN对环境的感知能力，使其在资源竞争条件下能够做出更明智的切换决策。

### 4.6.4 强化学习方法的局限性分析

尽管DQN方法在多种场景中展现出了与增强启发式算法相当甚至更优的性能，但客观分析其在当前实现中仍存在若干局限性。首先，DQN的训练需要大量的环境交互（本实验中为1000个episode），训练时间和计算资源开销显著高于启发式算法（后者无需训练，可直接执行）。在需要快速部署或频繁调整网络配置的实际场景中，训练开销可能成为部署障碍。

其次，DQN采用单智能体架构，仅控制一架目标无人机的切换决策，而增强启发式算法同时对所有无人机进行集中式决策。这一差异导致DQN在全局负载调控方面处于天然劣势——单智能体无法像集中式算法那样通过全局视角优化所有无人机的资源分配。在资源严重不足的极高密度场景中，这一局限尤为明显。

第三，DQN的决策过程缺乏可解释性。增强算法的效用函数和切换阈值具有明确的物理含义和设计逻辑，网络工程师可以根据经验进行调试和优化。而DQN的决策依赖于网络权重参数，其决策逻辑难以用人类可理解的方式表达，这在需要严格合规审查的电信系统中可能成为障碍。

第四，DQN的性能对训练数据和场景参数较为敏感。当实际部署环境的参数分布与训练环境存在较大差异时（例如基站数量变化、新的业务类型出现等），DQN模型可能需要重新训练或进行迁移学习，而增强算法凭借其基于规则的决策逻辑，具有更好的开箱即用能力。

尽管存在上述局限，DQN方法作为数据驱动决策范式的代表，展现了若干启发式算法难以实现的特性。最突出的一点是DQN能够自主发现切换策略中难以通过人工设计捕捉的非线性关系和长期收益。例如，DQN在低密度场景中学到的"保守切换"策略（减少不必要的切换操作），就是一种需要综合考虑切换收益与切换风险的长远视角，而启发式算法通常基于即时效用进行决策。此外，DQN具备通过持续学习适应环境变化的能力——随着更多数据的积累和训练的持续，DQN的决策策略可以不断优化，而启发式算法的规则集一旦确定便趋于静态。从长远来看，随着强化学习算法和硬件计算能力的持续发展，数据驱动方法在无人机切换决策领域的应用前景值得期待。

## 4.7 本章小结

本章通过五组递进式实验对所设计的无人机业务识别与切换决策联动系统进行了全面验证。实验一证实了业务识别准确性对系统整体服务质量的显著影响，识别准确率从33%提升至100%可使整体满意度提升28.6%，加权满意度提升38.5%，为"业务感知"切换决策框架的核心价值提供了定量依据。实验二和实验二b通过系统化的消融实验，验证了增强算法中各机制的独立贡献与协同效果，其中动态切换阈值是性能贡献最大的单一机制，可使切换成功率提升19.4%，负载方差降低92.8%。实验三在标准仿真条件下对增强算法与传统算法进行了全面对比，结果显示增强算法在切换成功率（+16.2%）、整体满意度（+9.6%）、加权满意度（+9.5%）、负载均衡度（+94.4%）和总负载（+18.3%）等核心指标上均实现了具有统计显著性的改善。实验四将对比扩展至五个差异化应用场景，验证了增强算法的泛化能力——在资源受限的农业监测场景中优势最为显著（满意度提升45.6%），在资源充裕的智慧城市场景中优势相对温和但仍保持切换成功率100%的鲁棒性。实验五引入DQN强化学习方法，在不同无人机密度条件下与启发式算法进行了对比，并通过特征消融实验验证了DQN状态空间增强设计的有效性，同时客观分析了DQN方法在训练开销、全局调控、可解释性等方面的局限性。

综合全部实验结果，本章的核心结论可以概括为：所设计的"识别—决策—评估"三层联动架构有效可行，业务感知增强切换算法在多种场景条件下均优于传统3GPP A3算法，业务识别准确性的提升对差异化服务质量保障至关重要，而基于深度强化学习的数据驱动决策范式在无人机切换场景中展现出了与启发式算法相当的性能水平和独特的自适应优势，为未来的持续研究和工程应用提供了有价值的参考方向。

## 参考文献

[1] Mnih V, Kavukcuoglu K, Silver D, et al. Human-level control through deep reinforcement learning[J]. Nature, 2015, 518(7540): 529-533.

[2] 3GPP TS 38.331. NR Radio Resource Control (RRC) protocol specification[S]. 3rd Generation Partnership Project, 2021.

[3] Mozaffari M, Saad W, Bennis M, et al. A tutorial on UAVs for wireless networks: Applications, challenges, and open problems[J]. IEEE Communications Surveys & Tutorials, 2019, 21(3): 2334-2360.

[4] Chen M, Saad W, Yin C. Virtual reality over wireless networks: Quality-of-service model and learning-based resource management[J]. IEEE Journal on Selected Areas in Communications, 2018, 36(11): 2613-2627.

[5] Sutton R S, Barto A G. Reinforcement learning: An introduction[M]. 2nd ed. Cambridge: MIT Press, 2018.

[6] Wu Q, Zeng Y, Zhang R. Joint trajectory and communication design for multi-UAV enabled wireless networks[J]. IEEE Transactions on Wireless Communications, 2018, 17(3): 2109-2121.

[7] Hassoun M H. Fundamentals of artificial neural networks[M]. Cambridge: MIT Press, 1995.

[8] 3GPP TS 36.331. Evolved Universal Terrestrial Radio Access (E-UTRA) Radio Resource Control (RRC) protocol specification[S]. 3rd Generation Partnership Project, 2020.

[9] Wang X, Garg S, Lin H, et al. UAV-assisted wireless communication for smart cities: A comprehensive survey[J]. IEEE Internet of Things Journal, 2021, 8(17): 13295-13317.

[10] Zhang J, Zeng Y, Zhang R. Spectrum and energy efficiency maximization in UAV-enabled mobile relaying[J]. IEEE Transactions on Communications, 2018, 66(2): 745-758.
