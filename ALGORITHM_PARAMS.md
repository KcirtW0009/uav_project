# 算法参数配置说明

## 1. EnhancedHandoverAlgorithm 参数

### 核心权重参数

| 参数名 | 默认值 | 范围 | 说明 |
|--------|--------|------|------|
| `w_sinr` | 0.45 | [0, 1] | SINR权重,控制信号质量对决策的影响程度 |
| `w_load` | 0.25 | [0, 1] | 负载权重,控制基站负载对决策的影响程度 |
| `w_rate` | 0.30 | [0, 1] | 速率匹配权重,控制速率匹配对决策的影响程度 |

### 切换阈值参数

| 参数名 | 默认值 | 范围 | 说明 |
|--------|--------|------|------|
| `base_threshold` | 0.005 | [0, 0.01] | 基础切换阈值,只有当效用提升超过此值才切换 |
| `threshold_lower_bound` | 0.001 | [0, 0.005] | 切换阈值下界,防止阈值过低导致频繁切换 |
| `confidence_factor_coeff` | 0.002 | [0, 0.01] | 置信度因子系数,识别置信度低时降低切换阈值 |
| `mobility_factor_coeff` | 0.003 | [0, 0.01] | 移动性因子系数,移动快时降低切换阈值 |
| `priority_factor_control` | 0.003 | [0, 0.01] | 控制信令优先级因子,高优先级业务降低切换阈值 |

### 探索参数

| 参数名 | 默认值 | 范围 | 说明 |
|--------|--------|------|------|
| `epsilon` | 0.05 | [0, 0.1] | 探索率,以一定概率随机选择基站,避免局部最优 |

### 紧急切换参数

| 参数名 | 默认值 | 范围 | 说明 |
|--------|--------|------|------|
| `emergency_sinr_threshold` | -5 | [-10, 0] | 紧急切换SINR阈值(dB),低于此值触发紧急切换 |
| `emergency_satisfaction_threshold` | 0.7 | [0.5, 0.9] | 紧急切换满足率阈值,低于此值触发紧急切换 |

### 基站过滤参数

| 参数名 | 默认值 | 范围 | 说明 |
|--------|--------|------|------|
| `bs_filter_sinr_threshold` | -10 | [-15, -5] | 基站过滤SINR阈值(dB),低于此值的基站被过滤 |
| `bs_filter_distance_ratio` | 2.5 | [1.5, 3.0] | 基站过滤距离倍数,距离当前基站超过此倍数被过滤 |

### 业务权重配置

不同业务类型对SINR、负载、速率的敏感度不同:

```python
BusinessType.CONTROL_SIGNAL: {'sinr': 0.5, 'load': 0.2, 'rate': 0.3}
BusinessType.VIDEO_STREAMING: {'sinr': 0.3, 'load': 0.25, 'rate': 0.45}
BusinessType.ENVIRONMENT_MONITORING: {'sinr': 0.25, 'load': 0.25, 'rate': 0.5}
```

- **控制信令**: 对SINR最敏感(0.5),其次是速率(0.3),对负载不敏感(0.2)
- **视频流**: 对速率最敏感(0.45),对SINR要求中等(0.3)
- **环境监测**: 对速率最敏感(0.5),对SINR要求较低(0.25)

## 2. NetworkEnvironmentWithRecognition 参数

### 基本参数

| 参数名 | 默认值 | 范围 | 说明 |
|--------|--------|------|------|
| `num_bs` | 8 | [4, 20] | 基站数量 |
| `num_uav` | 50 | [10, 200] | UAV数量 |
| `scenario` | 'default' | str | 场景类型: 'default', 'urban', 'emergency', 'agriculture' |

### SINR更新参数

| 参数名 | 默认值 | 范围 | 说明 |
|--------|--------|------|------|
| `enable_incremental_sinr_update` | True | bool | 是否启用增量SINR更新 |
| `sinr_update_interval` | 3 | [1, 10] | 完全SINR更新间隔(步数) |

**增量更新说明**:
- 启用增量更新后,只在UAV位置发生显著变化(>5米)时更新其SINR
- 每`sinr_update_interval`步进行一次完全更新
- 可显著降低计算复杂度,特别是UAV移动速度较慢时

### 中断检测参数

| 参数名 | 默认值 | 范围 | 说明 |
|--------|--------|------|------|
| `interruption_threshold` | 0.3 | [0.2, 0.5] | 满足率阈值,低于此值视为中断 |
| `interruption_duration` | 5 | [3, 10] | 持续步数,低于阈值且持续N步才计为中断 |
| `control_signal_threshold` | 0.4 | [0.3, 0.6] | 控制信令业务的中断阈值(更高要求) |
| `control_signal_duration` | 3 | [2, 5] | 控制信令业务的持续步数(更严格要求) |

## 3. 业务识别参数

### 识别更新器参数

| 参数名 | 默认值 | 范围 | 说明 |
|--------|--------|------|------|
| `min_update_interval` | 5 | [1, 20] | 最小更新间隔(步数) |
| `drift_threshold` | 0.25 | [0.1, 0.5] | 漂移检测阈值 |

## 4. 参数调优建议

### 场景1: 高密度UAV (num_uav > 100)
- 增加`w_load`到0.35,更重视负载均衡
- 降低`base_threshold`到0.003,更积极地切换
- 增加`sinr_update_interval`到5,减少SINR更新频率

### 场景2: 高移动性UAV
- 增加`mobility_factor_coeff`到0.005,更快响应移动
- 降低`bs_filter_distance_ratio`到2.0,考虑更多候选基站
- 降低`emergency_sinr_threshold`到-8,更保守的紧急切换

### 场景3: 低信噪比环境
- 增加`w_sinr`到0.55,更重视信号质量
- 增加`bs_filter_sinr_threshold`到-8,考虑更多基站
- 增加`emergency_sinr_threshold`到-3,更早触发紧急切换

### 场景4: 控制信令为主
- 降低控制信令的切换阈值
- 增加控制信令的SINR权重到0.6
- 提高紧急切换满足率阈值到0.8

## 5. 性能优化参数

### 降低决策时间
- 启用基站过滤: `bs_filter_sinr_threshold = -10`
- 限制候选基站数量: `bs_filter_distance_ratio = 2.0`
- 减少降级比例选项: 只使用[1.0, 0.8, 0.6]

### 降低SINR更新开销
- 启用增量更新: `enable_incremental_sinr_update = True`
- 增加完全更新间隔: `sinr_update_interval = 5`
- 减少日志大小: `max_decision_log_size = 200`

## 6. 评估指标

### 性能指标
- **平均决策时间**: 决策耗时(ms),越低越好
- **平均切换延迟**: 切换执行耗时(ms),越低越好
- **切换成功率**: 成功切换次数/总切换次数,越高越好

### 质量指标
- **平均满足率**: UAV满足率平均值,越高越好
- **加权满足率**: 按优先级加权的满足率,越高越好
- **关键业务满足率**: 控制信令等关键业务的满足率,越高越好

### 稳定性指标
- **切换次数**: 总切换次数,适中为佳(太少可能导致服务质量低)
- **中断率**: 处于中断状态的UAV比例,越低越好
- **负载方差**: 基站负载的方差,越低越好(负载均衡)
- **SINR波动**: SINR变化幅度,越低越好(信道稳定)
